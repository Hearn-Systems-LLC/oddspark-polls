---
baseline_commit: ca3713ef6fa53d4ad7e19e351404615b83a93ddf
---

# Story 1.10: Motion System & Chart Toggle

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->
<!-- Branch provenance: cut `story/1-10-motion-system-chart-toggle` from fetched `origin/main`. At story creation, `origin/main` is `ca3713ef6fa53d4ad7e19e351404615b83a93ddf` (the merge of Story 1.9); if main has moved by dev start, record the actual branch-cut SHA here. -->

## Story

As a Poll watcher,
I want vote arrivals to animate with the product's five motion primitives,
so that watching an opinion take shape feels alive — the product's signature moment.

## Acceptance Criteria

1. **Given** a Vote arriving on an open bar Tally, **When** the update lands, **Then** the affected bar's width transitions over 480ms on the spec easing while every other bar settles simultaneously (one synchronized settle, never a cascade), the changed bar's leading edge sparks 2px→4px→2px over 180ms timed with the width change, and counts tick over 400ms in monospace with zero reflow (UX-DR4).
2. **Given** updates arriving faster than one animation window, **When** they land, **Then** they coalesce into a single transition to the latest value — animations never queue or chain.
3. **Given** a leadership change, **When** it occurs, **Then** the new leader cross-fades blue→gold over 240ms while the deposed leader fades gold→blue concurrently and the `◆` moves with the gold; an exact tie withdraws gold and `◆` from every bar under `TIED`.
4. **Given** `prefers-reduced-motion: reduce`, **When** any of the five primitives would fire, **Then** every state change lands instantly and completely — widths snap, numbers snap, spark omitted, leader colors change on the frame, live dot holds steady. Reduced motion never reduces information (UX-DR4/17).
5. **Given** the `BARS · PIE` chart-form toggle, **When** a viewer switches to PIE, **Then** the same values render as static percentages with a `◆`-marked legend, no motion of any kind including on live updates, the choice does not persist across loads, and initial page paint never animates in either form (UX-DR5).
6. **Given** a Voter's own successful submission, **When** the confirmation renders, **Then** their bar sparks as the confirmation renders — the one moment the product exists to deliver.

## Tasks / Subtasks

- [x] Task 1: Arm the CSS motion foundation — bar transition, leader cross-fade, spark, reduced-motion, initial-paint gate (AC: #1, #3, #4, #5-initial-paint)
  - [x] UPDATE `src/styles/tokens.css`: add `--results-bar-leading-edge-width-spark: 4px` (DESIGN.md:178 declares `leadingEdgeWidthSpark`; tokens.css has no spark width) and the complete `chart-form-toggle` token family from DESIGN.md:193-203 (typography label-caps, `color` dim, `colorCurrent` solar-ink, separator `·`, gap spacing-2, borderBottom hairline rule, paddingY spacing-2, minHeight 48px, focus outline/offset 2px/2px). Follow the existing four-place mode-collapse structure (source `-dark`/`-light` pairs at `:12-52`, dark runtime `:156-176`, light runtime `:179-200`, both `prefers-color-scheme` override blocks `:203-247`). Update the stale comment at `:87` ("nothing animates in story 1.1") — the motion tokens are consumed from this story on. NEVER delete or rename `--color-solar-dark`: `scripts/smoke.mjs` extracts it and asserts the served HTML carries that hex — a rename breaks the deploy gate by design (AGENTS.md:282-287)
  - [x] UPDATE `src/components/results-bar.astro`: declare the motion CSS gated behind an arming class — e.g. `.results-tally.is-motion-armed .results-bar-fill { transition: width var(--motion-bar-transition) var(--motion-ease), background-color var(--motion-leader-crossfade) var(--motion-ease), border-right-color var(--motion-leader-crossfade) var(--motion-ease); }`. The gate is mandatory, not stylistic: `tests/e2e/results.spec.mjs:861-865` asserts `transitionDuration === "0s"` on an idle warm-loaded bar, which is the executable form of "initial page paint never animates" (DESIGN.md:568, epics.md:515) — an unconditional transition declaration fails it even when nothing moves. The arming class is applied lazily by script with the first ordinary in-cadence reconcile (see Task 2) and NEVER by server render. Scope every new rule away from `[data-tally-skeleton]` (`results.spec.mjs:806-812` asserts skeleton `{ transition: "0s", animation: "none" }`)
  - [x] Spark: `@keyframes` on `border-right-width` (2px → `var(--results-bar-leading-edge-width-spark)` → 2px) over `var(--motion-spark)`, fired via a short-lived class (e.g. `is-spark`) on the fill. The leading edge IS `border-right` on `.results-bar-fill` (`results-bar.astro:93`) — there is no separate edge element; do not add one. `[ASSUMPTION: spark easing]` no easing is specified for the spark; use `ease-in-out` — the symmetric-loop rationale that ratified `ease-in-out` for the pulse in Story 1.9 (`motion.ease` is a fast-out settle curve, wrong for a symmetric 2→4→2 flash) applies identically. Note `is-zero` removes the edge entirely (`:97-99`); a bar leaving zero gains its edge and sparks from it — acceptable, no special case
  - [x] Leader cross-fade needs NO new elements: leader state is `background` + `border-right-color` on the fill (`results-bar.astro:101-104`), both transitionable. The `◆` marker moves by the existing `hidden` toggle (`results-live.ts:358-360`) and does not itself animate — the gold cross-fade on fills/edges is the motion; the marker changes on the frame (DESIGN.md:558: composited wash contrast is 1.12:1, so the marker, not hue, carries leadership). Exact tie: existing `is-leader` withdrawal on every bar + `TIED` line — already reconciled by 1.9; the cross-fade transition simply animates the withdrawal
  - [x] Reduced motion: one `@media (prefers-reduced-motion: reduce)` block zeroing every new transition/animation (`transition: none; animation: none`), following the live-indicator precedent (`src/components/live-indicator.astro:81-86` — the only existing reduced-motion CSS, guarded by `tests/unit/live-indicator.test.mjs:57-60`). CSS cannot stop the JS count-up — that guard is Task 2's `matchMedia`
  - [x] Add `font-variant-numeric: tabular-nums` to `.results-bar-value` — Courier Prime is monospace so digits are already fixed-width, but the explicit declaration protects the fallback stack ("zero reflow", DESIGN.md:564). Do not change `.results-bar-value` color: the count deliberately inherits `text`, never `dim` (`countColor` was deleted for contrast — DESIGN.md:556, review-accessibility.md:21)
  - [x] Unit-first: extend the source-contract suites (`tests/unit/` reading component source, per `live-indicator.test.mjs` idiom) — motion rules reference tokens by `var(--motion-*)` name (never literal ms — DESIGN.md:566: "a timing change is one edit"), transitions are gated behind the arming class, the reduced-motion block exists and neutralizes them, skeleton selectors untouched

- [x] Task 2: Wire motion into the live reconciler — arming, spark trigger, count-up, coalescing, snap contexts (AC: #1, #2, #3, #4)
  - [x] Architectural shape, ratified: the spine's capability map routes "UX live motion" through `scripts/results-live.ts` (`ARCHITECTURE-SPINE.md:513`) — extend that file for all motion wiring; add a NEW pure sibling `src/scripts/results-motion-core.ts` (mirroring the `results-live-core.ts` two-file pattern) for Node-unit-testable logic: count-up interpolation/retargeting math, eased-progress helper on the `motion.ease` curve, reduced-motion decision, spark-eligibility rule (value **increased** — DESIGN.md:564 sparks on increase, not on any change). Do NOT create a general `motion.ts` module — nothing else in the product animates (UX-DR4)
  - [x] Arming: keep `[data-results-tally]` unarmed through initial paint, then add `is-motion-armed` only with the first ordinary in-cadence reconcile. CRITICAL — snap contexts stay snapped: PIE form, visibility return, `online`/`pageshow` recovery (including 304), reconnect after stale, the reload fallback, the closed final snapshot, and a runtime switch to reduced motion all SNAP to current values (EXPERIENCE.md:270 "the truth, not a highlight reel"; UX-DR15 "snap to current on resume"). One explicit `withSnappedMotion` mechanism clears active sparks, snaps numeric slots, applies values, flushes, and restores the prior arming state; only ordinary in-cadence BARS reconciles animate
  - [x] Spark trigger: the width-change guard at `results-live.ts:327-331` is the changed-bar detector — when the option's count increased, restart the `is-spark` class on that bar's fill (remove class → force reflow → re-add, or `getAnimations()` cancel+restart) so rapid coalesced updates re-fire cleanly and animations never queue. Spark starts on the same frame as the width write so "the brightening reads as the cause of the movement" (EXPERIENCE.md:275). Only the changed bar sparks; every bar's width transitions (percentages move even when counts don't — one synchronized settle falls out of writing all `--bar-width` values in one reconcile pass, EXPERIENCE.md:274)
  - [x] Count-up (400ms): rAF tween replacing the direct `textContent` writes at `results-live.ts:332-341` (percent + count) and `:375-378` (totals line). Retarget on new data from the currently displayed value — never restart from the old value, never queue (EXPERIENCE.md:279 "from wherever it currently is"; CSS transitions retarget free, the JS tween must cancel-and-retarget explicitly). Guard with `matchMedia("(prefers-reduced-motion: reduce)")` → snap. Percent/count spans inside the `role="img"` bar are presentational to AT (the bar's name is its `aria-label`) — keep the `aria-label` write at `:342-350` immediate and final-valued; a ticking accessible name would spam AT and break every `getByRole("img", { name: … })` e2e locator
  - [x] THE CHATTER TRAP — totals line: `[data-live-total]` sits INSIDE the one polite `aria-live` region (`results-tally.astro:87-104`), and EXPERIENCE.md:277 says it ticks with the counts. Tweening announced text = one announcement per animation frame. `[ASSUMPTION: totals-line split — RATIFIED]` tick an `aria-hidden="true"` visual span while writing the final value exactly once per reconcile to the region's announced text (visually-hidden), preserving 1.9's single-announcement semantics ("122 votes"), the region node's persistence (`live-results.spec.mjs:339-342,648-651` persistence tokens), exactly one `aria-live` on the surface (`live-indicator.test.mjs:80-82`), and the source-order assertion (`:88-92`). Leader-change (`Pizza now leading, 47 percent.`) and `TIED` announcements are 1.9 behavior — unchanged
  - [x] Multi-select summary (`{n} VOTERS · {m} SELECTIONS`): if its counts join the count-up, update BOTH the SSR line (`results-tally.astro:77`) and the poller mirror (`results-live.ts:364`) together — the reconciliation diff-check at `:365` desyncs if one side changes alone (deferred-work.md:5, the un-pluralized `1 VOTERS` finding is deferred — do not silently fix it here unless both sides move together and the diff-check is updated in the same commit)
  - [x] Preserve byte-for-byte: announcement queue (`results-live.ts:169-211`), stale/connected transitions (`:213-258`), reload fallback + circuit breaker (`:284-294`), closed terminal state (`:497-499`), `previousLeaderId` capture (`:296-298`), `YOUR BALLOT` (never updates live, never gold), option-order reload rule. A failed poll still must not log — the e2e console collector fails the suite on any console error
  - [x] Unit coverage (`tests/unit/results-motion-core.test.ts`): interpolation endpoints exact (never overshoots, lands on the true final value), retarget-from-current under coalescing, eased progress monotonic, reduced-motion → zero-duration decision, spark only on increase (equal/decrease/no-op excluded), snap-context decisions

- [x] Task 3: Ship the `chart-form-toggle` — component, script, placement (AC: #5)
  - [x] NEW `src/components/chart-form-toggle.astro` + NEW `src/scripts/chart-form-toggle.ts` — the toggle is its own sanctioned hand-written-JS concern (UX-DR25, epics.md:116, readiness report `:206`), separate from the poller. Visual contract (DESIGN.md:570-574): two label-caps words `BARS · PIE` above the bar group with a hairline beneath — current form `solar-ink`, other `dim`, aria-hidden `·` separator, 48px min row height, 2px/2px focus ring via the new tokens. No icon, no segmented control, no pill, no box. Never opacity as the state mechanism (UX-DR6 principle) — the state is the color swap
  - [x] `[ASSUMPTION: toggle ARIA pattern — RATIFIED]` no ARIA pattern is prescribed anywhere in the UX package. Use two native `<button type="button">` elements with `aria-pressed`, inside a group labelled via `aria-label="Chart form"` — native buttons under decorative presentation match the system's stated preference (DESIGN.md:540, EXPERIENCE.md:251), Tab-reachable in reading order (EXPERIENCE.md:221), words-not-icons (EXPERIENCE.md:260). Record any deviation in completion notes, don't invent a third pattern
  - [x] Placement: inside `results-tally.astro`'s `[data-tally-final-region]`, immediately before `.results-tally-bars` (`:105`) — being inside results-tally means it renders only on `visible` branches automatically, satisfying the hidden-surface leak bans (`results.spec.mjs:272-290` bans `◆`/bar literals from hidden HTML; `:403-404` bans buttons in hidden `<main>`; same guard `live-results.spec.mjs:550-557`). Two structural constraints: (a) `.results-tally-final-region` is `display: contents` (`:205-207`), so at ≥1024px the toggle becomes a direct grid item of the post-vote two-column grid — add its `grid-column`/`grid-row` rule to `src/pages/[reference].astro:1107-1135` or it lands unplaced; (b) the inline cold-load enhancer resolves its root via `document.currentScript.parentElement` (`results-tally.astro:136-137`) — add siblings freely, never nest or move that script. Keep the source-order assertion intact (`live-indicator.test.mjs:88-92`: summary → live region → bars)
  - [x] Server-render the toggle `hidden` and reveal it from `chart-form-toggle.ts` (the vote-hint idiom, `[reference].astro:888-892`) — a no-JS visitor gets the complete bar Tally and never a dead control (AD-2 no-JS floor). `hidden` needs the explicit `display: none` guard block (`results-tally.astro:247-254` — "hidden loses to author display rules"). Load the script wherever `results-tally` renders visible: `src/pages/[reference]/results.astro` visible branch and the post-vote surface in `src/pages/[reference].astro` (alongside, not inside, the `results-live.ts` conditional at `:924-930` — the toggle also serves effectively-closed Tallies where the poller never loads)
  - [x] Behavior: `BARS` default on every load, per-viewer, NOT persisted — no localStorage, no cookie, no server field, zero added network requests (epics.md:96,515; deferred-work.md:9 cost note). Switching is a view change in place: a synchronous chart-form event fires before PIE renders, allowing the poller to snap numeric slots and clear active sparks before `.results-tally-bars` is hidden; toggling back re-enters bars at current values with NO replay and no transition
  - [x] `[ASSUMPTION: multi-select suppression — RATIFIED]` on `multiSelectEnabled` Polls the toggle does not render and the Tally is bars-only. Per-option percentages are shares of VOTERS and sum past 100% (FR-7, DESIGN.md:556) — they cannot form an honest pie, and re-basing slices to selections would make PIE numbers disagree with BARS, violating "the same values render as static percentages" (epics.md:515). Record the decision visibly in completion notes
  - [x] Coverage: source-contract unit test for the component (tokens by name, `aria-pressed` pair, hidden-by-default, no `aria-live`, 48px/focus-ring tokens); the no-controls-inside-tally assertion at `live-results.spec.mjs:333-335` must be updated to allow EXACTLY the two toggle buttons and nothing else

- [x] Task 4: Render the static pie — geometry, legend, and its no-motion contract (AC: #5)
  - [x] The pie is the least-specified surface in the UX package — "static percentages with a ◆-marked legend" is the entire prose spec, no mockup exists, and no slice tokens exist. The following decisions are RATIFIED for this story; implement them, and record legibility concerns in completion notes rather than inventing alternatives mid-build
  - [x] `[ASSUMPTION: rendering technique]` inline SVG built with `document.createElementNS` from `chart-form-toggle.ts` (or authored SVG in the component patched by script). NOT a conic-gradient box: a circle via `border-radius: 50%` would breach the zero-radius rule, whose ONLY exceptions are the 6px live dot and Turnstile chrome (DESIGN.md:524,528,686) — the pie must be drawn geometry, its container and legend staying square, shadowless, gradient-free. `innerHTML`/template-string construction FAILS THE BUILD (`tests/unit/no-raw-html.test.mjs:13-22`) — DOM APIs only
  - [x] `[ASSUMPTION: slice colors]` token-bound only, no new colors: non-leader slices alternate `--color-entropy-wash` / the bar track background token so adjacent slices separate; every slice boundary carries a 1px `--color-rule` stroke (hairline idiom); the leader slice fills `--color-solar-wash`. Hue does NOT carry leadership (composited washes measure 1.12:1 — DESIGN.md:558): the ◆ in the legend is the leadership signal, exactly one per surface, same withdrawal-on-tie rule as bars (`results.spec.mjs:1109-1111` one-◆ assertions extend here). Slices run clockwise from 12 o'clock in creator option order (bars never reorder — UX-DR3; the legend maps by the same order); zero-percent options render no slice but keep their legend row
  - [x] `[ASSUMPTION: legend format]` one row per option in creator order: optional `◆ ` prefix (leader only, `solar-ink`), option label, then the `47% · 122` value cluster in the bar-value typography — never a percentage without its raw count (DESIGN.md:568 binds the pie legend too). Angles derive from the SERVER-computed exact unit share (`pieShare × 360°`) only; the rounded percentage remains display data and the client never recomputes a share from counts (AD-9/NFR-6; the boundary sentence belongs in a code comment). Only floating-point closure adjusts the final slice; rounded percentages are never normalized or rebased
  - [x] `[ASSUMPTION: data source]` bars carry `data-pie-share` / `data-percent` / `data-count` attributes — server-rendered in `results-bar.astro` and kept current by the poller in the same reconcile pass — and the pie renders geometry from the exact share while its legend renders the rounded percentage and raw count. This gives one uniform source whether the poller is loaded (open Poll) or not (closed Poll), with zero network requests. CAUTION: put the attributes on `.results-bar`, never as inline style on the track — `results.spec.mjs:891-894` asserts the track's `style` attribute EQUALS `--bar-width: 33%` exactly
  - [x] No motion of any kind in the pie view, ever, including live updates (epics.md:96,515; DESIGN.md:695): a live update while PIE is active lands as a plain re-render at the new values — no transition on slices, no count-up in the legend, no animated sweep on entry. Empty Poll (zero votes): no ring; the existing empty-state line plus zero-valued legend rows. Tie: no gold, no ◆ anywhere, the existing `TIED` line stays visible above the group in either form. `aria-live` announcements continue unchanged in pie view (the region is form-agnostic)
  - [x] `[ASSUMPTION: pie accessibility]` the SVG is `aria-hidden="true"` decorative; the legend list carries the accessible content with the bar accessible-name pattern per row ("Pizza, 47 percent, 122 votes, leading" — reuse `barAccessibleName` from `src/components/results-bar.ts`, never duplicate the format). No new interactive elements, no per-slice announcements
  - [x] Coverage: unit tests for angle derivation (sums to 360, rounding normalization, zero-slice omission, single-option full circle) in the pure core; contract tests for aria-hidden SVG + legend naming; e2e in Task 6

- [x] Task 5: The own-vote spark on the confirmation render (AC: #6)
  - [x] The post-vote flow is server-rendered POST→303→GET (`[reference].astro:432-446` → flash consume `:519-540` → `Counted.` outcome `:731-778` → `<ResultsTally>` `:903-916`); the Tally becomes visible when the cold-load enhancer reveals `[data-tally-final-region]` on its second `requestAnimationFrame` (`results-tally.astro:156-160`). Fire the spark AFTER that reveal — it is the ONE sanctioned exception to never-animate-on-initial-paint (EXPERIENCE.md:291: "the one animation the product exists to deliver"), and it must not disturb the focus contract (the outcome line, not the Tally, takes focus — `tabindex="-1"` + autofocus already shipped)
  - [x] The DOM cannot currently identify WHICH bar is the voter's: `yourBallotLabels` is a joined display string (`results-tally.astro:67-69`) and label-text matching is fragile. Add a `yourBallotOptionIds` prop to `ResultsTally` flowing from the `castSelectionIds` already computed at `[reference].astro:611-617`, emitting `data-your-option` on the matching bar row(s). This is an authorized-viewer fact on an already-visible surface — it must never appear on hidden branches (covered structurally, results-tally renders visible-only) and must NOT change the `YOUR BALLOT` line semantics (text-only, never gold, never a second ◆ — DESIGN.md:457)
  - [x] Fire from the enhancement layer so it works when `results-live.ts` is NOT loaded (a Poll that closed between vote and render still confirms with a spark — the poller conditional at `[reference].astro:924-930` excludes closed) — `[ASSUMPTION: hook location]` the chart-form-toggle script (always loaded on visible Tallies) or a small shared init in `results-live.ts` + fallback; pick one, state it in the Dev Agent Record. `[ASSUMPTION: multi-select]` every bar in the voter's ballot sparks once, simultaneously, same 180ms — "their bar" generalizes to their selections; no staggering (a cascade is banned choreography). Under reduced motion the spark is omitted entirely — the confirmation text IS the state change; nothing is lost (EXPERIENCE.md:291)
  - [x] Coverage: unit contract for the prop→attribute wiring (single and multi select, absent for non-voters); e2e in Task 6 proves the spark fires on the confirmation render and is omitted under reduced motion

- [x] Task 6: Update the no-motion-era assertions, prove everything end to end, close the gate (AC: all)
  - [x] Update the four assertions 1.9 wrote for the snap era — deliberately, not incidentally: `live-results.spec.mjs:320-324` (live-update bar `transitionDuration` `"0s"` → becomes `0.48s` armed / `0s` under reduced motion), `results.spec.mjs:861-865` (KEEP as the initial-paint guard — idle warm load must still report `0s` because arming happens post-paint and transitions live behind the armed class; verify the mechanism satisfies it rather than weakening the assertion), `live-results.spec.mjs:333-335` (no-controls-in-tally → exactly the two toggle buttons), `results.spec.mjs:806-812` (skeleton stays motionless — should pass untouched; treat a failure as a scoping bug, not a test to update). Also re-verify `live-results.spec.mjs:311-319` (inline `--bar-width` reads) and `results.spec.mjs:891-894` (exact track style equality) still hold with the implementation choices above
  - [x] NEW `tests/e2e/motion.spec.mjs` (or extend `live-results.spec.mjs`) with the established idioms — serial single worker over shared local D1 (`playwright.config.ts` — chromium only, port 4391), `scopedReference()`, `seedPoll`, `castVote`, `watchConsole` (any console error fails), `installLiveRecorder`, `captureProof`, and the rAF frame-control init script from `results.spec.mjs:772-791` for deterministic count-up/spark timing. Keep waits tight — never sleep out a backoff ceiling
  - [x] Motion proofs: after arming, a cross-context Vote produces `0.48s` transition-duration on fills and a spark class/animation on the changed bar only; counts land exactly on final values (frame-advance to completion — no overshoot, no stale intermediate); leader flip cross-fades (transition properties include background-color/border-right-color at `0.24s`) with the ◆ moving and exactly one ◆ visible; exact tie withdraws all gold/◆ under `TIED`; coalescing — two rapid updates yield one settle to the latest value, never a queued replay; visibility-return and stale-recovery refreshes SNAP (`0s` during the snap path)
  - [x] Reduced-motion proofs (`page.emulateMedia({ reducedMotion: "reduce" })`, idiom at `live-results.spec.mjs:252-261`): widths/numbers snap, no spark, leader colors change on the frame, dot holds full opacity, updates still land at full fidelity — reduced motion never reduces information
  - [x] Toggle/pie proofs: BARS default on every load (reload lands on bars — no persistence); PIE renders aria-hidden SVG + legend with counts and exactly one ◆; live update while in PIE re-renders statically (no transition/animation computed anywhere in the pie view); switching back re-enters bars at current values without replay; toggle absent on multi-select Polls; toggle hidden without JS; hidden surfaces (After Close pre-close, Creator-Only non-owner) still carry zero toggle/pie/◆/bar artifacts (`expectNoAggregateResultFacts` extended to any new literals); focus ring and `aria-pressed` states correct; 48px hit target
  - [x] Own-vote spark proof: cast a vote, assert the spark fires on the voter's bar as the `Counted.` confirmation renders (and on ALL selected bars for multi-select), outcome line still takes focus, reduced-motion omits the spark, and a Poll closed between vote and render still confirms (no poller, no errors)
  - [x] Browser proof is mandatory (AGENTS.md:121-124 — don't ask Justin to eyeball it): captures at 375px dark and ≥1024px light of the bar Tally mid-motion-era (post-update), the PIE view with legend, the toggle in both states, and the post-vote confirmation; console clean throughout
  - [x] UPDATE `CHANGELOG.md` under `## [Unreleased]` (user-facing motion + chart toggle entries). If the multi-select pluralization deferral is intentionally left, it stays in `deferred-work.md` — never a source TODO (AGENTS.md:338-339)
  - [x] Run narrow red/green tests during each task, then the repository gate in documented order (AGENTS.md:113-118): `pnpm migrations:guard`, `pnpm test`, `pnpm check`, `pnpm test:e2e`, `pnpm types`, `git diff --exit-code worker-configuration.d.ts`, `pnpm build:production`. No migration, dependency, binding, or endpoint change is expected in this story — the drift assertion stays in the gate

### Review Findings

- [x] [Review][Patch] Supply exact server-computed PIE geometry alongside rounded display percentages so every positive option retains an honest wedge [`src/scripts/chart-pie-core.ts:23`] — fixed with `pieShare`, `data-pie-share`, and rounded-zero/30-option proofs
- [x] [Review][Patch] Batch every bar width and leader mutation before one shared spark flush so the settle and cross-fade remain atomic [`src/scripts/results-live.ts:421`] — fixed with staged row plans, one mutation phase, and one root flush
- [x] [Review][Patch] Make PIE an explicit snap/no-motion poller state and snap all numeric slots before BARS re-entry [`src/scripts/chart-form-toggle.ts:214`] — fixed with the synchronous chart-form contract and poller snap path
- [x] [Review][Patch] Remove `is-spark` after completion so an old Vote cannot replay after a display or chart-form change [`src/scripts/results-live.ts:421`] — fixed with shared animation-end/fallback cleanup and explicit state-change clearing
- [x] [Review][Patch] Snap active motion when a visibility, online, or pageshow recovery returns 304 [`src/scripts/results-live.ts:658`] — fixed in the recovery-not-modified path and proven in browser coverage
- [x] [Review][Patch] Snap active count-ups when the reduced-motion preference changes to reduce [`src/scripts/results-live.ts:170`] — fixed with a media-query change listener that snaps every active slot
- [x] [Review][Patch] Preserve a visible spark when a positive count is rounded to zero percent [`src/components/results-bar.astro:110`] — fixed by deriving zero state from count rather than rounded percentage
- [x] [Review][Patch] Reserve stable numeric columns across digit-boundary count-ups so labels never reflow [`src/components/results-bar.astro:167`] — fixed with reserved percentage/count widths and monotonic count capacity
- [x] [Review][Patch] Update PIE legend rows in place so live refreshes preserve assistive-technology reading position [`src/scripts/chart-form-toggle.ts:109`] — fixed with option-keyed persistent legend rows
- [x] [Review][Patch] Drive two distinct client reconciles inside one animation window in the coalescing E2E proof [`tests/e2e/motion.spec.mjs:250`] — fixed with two routed 200 updates under virtual frame control
- [x] [Review][Patch] Exercise armed and 304 recovery paths in the snap E2E coverage [`tests/e2e/motion.spec.mjs:359`] — fixed with visibility recovery 304→200 browser coverage
- [x] [Review][Patch] Keep JavaScript count-up easing sourced from the named motion token's single source of truth [`src/scripts/results-motion-core.ts:83`] — fixed by parsing computed `--motion-ease` and passing the curve to every tween

## Dev Notes

### Product and UX Contract

- UX-DR4 verbatim (epics.md:95): exactly five primitives (`bar-transition` 480ms, `spark` 180ms, `count-up` 400ms, `leader-crossfade` 240ms, `pulse` 2400ms); synchronized settle across all bars; coalescing to latest value (animations never queue); leader change cross-fades gold between bars with the `◆` moving; `prefers-reduced-motion` snaps every state change instantly without losing information; idle is still; **nothing else in the product animates** [Source: `epics.md:95`; `DESIGN.md:564-566`; `EXPERIENCE.md:223,289`]
- Choreography canon is `EXPERIENCE.md:266-291`: affected bar transitions while every other bar settles simultaneously ("one synchronized settle, never a cascade", `:274`); spark starts WITH the width change so brightening reads as the cause (`:275`); counts AND percentages tick, and the totals line ticks with them (`:276-277`); coalescing retargets "from wherever it currently is" (`:279`); cross-fade runs concurrent with the width transition on both bars, ◆ moving with the gold (`:281`); reduced-motion contract complete at `:224` and `:283`
- The pulse already shipped in 1.9 (`live-indicator.astro`) — this story adds the other four primitives and MUST NOT touch the dot beyond leaving it alone
- Tie rule: exactly one bar may be gold; an exact tie withdraws gold and ◆ from EVERY bar under a label-caps-lg `TIED` line — "Gold means 'this one is winning,' and it must never lie" [Source: `DESIGN.md:560`; `EXPERIENCE.md:281`]
- The ◆ is mandatory because hue cannot carry leadership: solar-wash vs entropy-wash composited is 1.12:1, the edges 1.38:1 [Source: `DESIGN.md:558`; `review-accessibility.md:23`]
- Counts/percentages stay `text` color — `countColor: dim` was deleted at 2.80:1/2.50:1 over the washes [Source: `DESIGN.md:556`; `review-accessibility.md:21`]
- Motion is verified NOT a photosensitivity risk (180ms brightness change on a 2px×34px element; coalescing caps flash rate; the pulse's WCAG 2.2.2 exposure is resolved by the reduced-motion hold) [Source: `review-accessibility.md:103`]
- UX-DR5 verbatim (epics.md:96): `BARS · PIE` text toggle above the Tally; bars default on every load; per-viewer, not persisted; pie renders static percentages with a `◆`-marked legend and no motion of any kind including on live update. Toggle visual spec at `DESIGN.md:570-574`; token block at `DESIGN.md:193-203`; "no pie as default, no live-update animation in the pie view" on the Don't list [Source: `DESIGN.md:695`]
- The own-vote spark is the product's climax: "On a successful submission, the confirmation and the Tally render together, and the Voter's own bar sparks… the first thing they see — and the outcome line takes focus as it renders, so it is the first thing they hear" [Source: `EXPERIENCE.md:291,352`] — "The spark in the results bar is the entire celebration budget" [Source: `DESIGN.md:692`]
- Copy: `Counted.` / `Results are live, updating as they arrive.` (`EXPERIENCE.md:83`); `TIED`; leader announcement `Pizza now leading, 47 percent.` (`EXPERIENCE.md:254`); toggle words are the literals `BARS` and `PIE` with `·` — no other new copy, no exclamation marks
- Mockup evidence (`mockups/key-tally.html`, the only rendered artifact — static, no motion/toggle/pie markup exists anywhere): bar anatomy at `:124-143` (fill with `border-right` edge), value cluster at `:229` (`◆ ` as a separate span prefixing the percent). Two places the spec beats the mockup: leader edge is `solar-ink` per tokens (mockup's `--solar` at `:142` is wrong in light mode — DESIGN.md:180,405), and the 1px `baselineRule` is required (DESIGN.md:173,554) though the mockup fakes it with gaps

### Architecture Guardrails

- The spine's ONLY motion line is the capability map row: "UX live motion and trust surfaces | server result projection plus `scripts/results-live.ts` | AD-2, AD-8, AD-10" — motion extends the existing poller; there is no sanctioned standalone motion module. The chart-form toggle IS separately sanctioned hand-written JS (UX-DR25) [Source: `ARCHITECTURE-SPINE.md:513,414`; `epics.md:116`]
- AD-2 (SPINE:56-64): server-rendered HTML, zero client JS by default, isolated vanilla TS enhancements only, no hydrated SPA. NFR-5 (epics.md:56): no heavy client payload — NO animation library, NO charting library; CSS transitions + rAF + SVG cost nothing
- AD-9/NFR-6/AR-7: no result math on the client — the pie consumes server-computed percentages; percent→angle is geometry, not tabulation [Source: `ARCHITECTURE-SPINE.md:169-178`; `epics.md:57,74`]
- Zero radius everywhere; no shadows, no gradients; the only round things are the 6px live dot and Turnstile's chrome — the pie is drawn geometry (SVG), never a border-radius box [Source: `DESIGN.md:513,524,528,686-687`]
- Tokens change in `src/styles/tokens.css`, never inline in a component; DESIGN.md frontmatter is the source and tokens.css its code expression [Source: `AGENTS.md:255-258`]. All four needed durations + easing ALREADY EXIST unconsumed at `tokens.css:87-93`; bar component tokens at `:142-149`; `--rounded-full` is the only surviving radius (`:100-105`)
- AD-10's transport is untouched: 3s cadence, visible-only, coalesced versions, stop on close — 1.9 shipped it; 1.10 changes presentation only. The 3000ms cadence vs 480ms window means natural coalescing is rare — test it deliberately with rapid updates, not incidentally [Source: `ARCHITECTURE-SPINE.md:180-192`]
- Explicit prohibitions: no WebSockets/DO (SPINE:520, AR-21), no new endpoint, no persistence of the toggle choice anywhere, no bar reordering (UX-DR3), no percentage without its raw count, no `innerHTML`/`set:html` (build-failing test), no migration/binding change, no sixth animation
- AR-20 test layout (NOTE: the `AR-*` list lives in `epics.md:68-88`, NOT in the SPINE — `AGENTS.md:249-252`'s claim otherwise is drift; cite epics.md): `tests/unit` pure logic (Node), `tests/integration` workerd (none expected this story — no server change), `tests/e2e` Playwright [Source: `epics.md:87`; `ARCHITECTURE-SPINE.md:434-438`]
- No axe run exists in CI; accessibility beyond the mechanical is human judgment against the UX artifacts, and visual proof with screenshots is mandatory for browser-observable changes [Source: `AGENTS.md:121-127`]

### Previous Story Intelligence (1.9)

- The reconciler (`src/scripts/results-live.ts` `reconcile()` `:280-393`) is the motion attachment surface. Patch sites: width via `--bar-width` on the track with an inequality guard (`:327-331` — this guard IS the changed-bar/spark detector), percent/count `textContent` (`:332-341`), `aria-label` final write (`:342-350`), `is-leader` toggle (`:351-353`), `is-zero` (`:354-357`), ◆ `hidden` toggle (`:358-360`), multi-select summary (`:364`, diff-check `:365`), totals line (`:375-378`). `previousLeaderId` captured at `:296-298`. Structural mismatch → `reloadOnce()` with circuit breaker (`:284-294`)
- `results-live-core.ts` exports the transport pure core (cadence/backoff/validator/tri-state) — motion math goes in a NEW pure sibling, not in there; the 1.9 review added the reload circuit breaker, 204/404 terminal handling, and closed-recovery announcement fixes — do not regress them (story file `1-9-live-updating-results.md` Review Findings)
- Bar anatomy (`results-bar.astro:44-74`): `role="img"` root with `data-option-id` / `aria-label`; `.results-bar-track[style="--bar-width: N%"]` → `.results-bar-fill` (edge = `border-right`, `:93`; `is-zero` removes it `:97-99`; leader = background + border-color `:101-104`) + `.results-bar-content` → label + value cluster (◆ span, pct, count). Pure helpers `barWidthPercent`/`barAccessibleName` in `results-bar.ts` — import, never duplicate. Header comment `:9` ("no CSS transition in this story") must be updated
- Tally structure (`results-tally.astro`): root data attrs `:55-62`; `YOUR BALLOT` `:64-72`; final region `:73-118` (summary `:77`, tied, empty, ONE polite region `:87-104`, bars `:105-117`); skeleton `:119-132`; inline enhancer `:133-171` (currentScript parent `:136-137`, reveal on second rAF `:156-160`, fail-open catch `:161-167` — motion arming must NEVER sit between the viewer and the authorized result); `hidden` display guards `:247-254`
- Post-vote page (`[reference].astro`): flash-cookie GET render, outcome autofocus `:731-778`, ResultsTally `:903-916`, `vote-form.ts` always `:923`, `results-live.ts` only open+visible `:924-930`, post-vote grid rows `:1107-1135` (new region children need explicit placement), `castSelectionIds` `:611-617`
- E2E house idioms: serial ONE worker over shared local D1; `scopedReference()`/`seedPoll`/`castVote`/`watchConsole`/`installLiveRecorder`/`captureProof` in `live-results.spec.mjs:23-204`; reduced-motion emulation precedent `:252-261`; rAF frame control `results.spec.mjs:772-791`; exact copy asserted byte-for-byte; no-leak assertions scope to `<main>`; telemetry never carries the public reference; record the actual branch-cut SHA in frontmatter
- 1.9's snap-era assertions that 1.10 must consciously revisit are enumerated in Task 6 — every one is there because a 1.9 AC demanded stillness; each update must map to a 1.10 AC that now demands motion
- Unit-test idioms: source-contract tests read `.astro`/`.ts` source with `readFileSync` and assert token names/regex ordering (`live-indicator.test.mjs`, `results-live-contract.test.mjs`); pure logic in plain vitest on Node; both `.test.ts` and `.test.mjs` live in the unit project; fast-check 4.9.0 available (count-up interpolation invariants are a natural property target)

### Current Technical Information

- Pinned stack, no changes and NO new dependencies: Astro 7.1.5 + `@astrojs/cloudflare` 14.1.6, TypeScript 7.0.2, Vitest 4.1.10, Playwright 1.62.0, Node 24.18.0, pnpm 11.17.0. Everything this story needs is baseline browser platform — no external research gaps: CSS transitions (retargeting interpolates from current computed value — coalescing for free), `border-right-width` is animatable, `@keyframes` restart via class re-add + reflow or `Element.getAnimations()`, `matchMedia("(prefers-reduced-motion: reduce)")` (+ `change` listener), `font-variant-numeric: tabular-nums`, `requestAnimationFrame` tweening, `document.createElementNS` for SVG. No `@property` registration needed (nothing animates a custom property itself; `--bar-width` changes discretely and the fill's `width` transition does the interpolating)
- Playwright: `page.emulateMedia({ reducedMotion: "reduce" })` and `getComputedStyle(...).transitionDuration` assertions are the proof mechanics; only chromium is configured — nothing cross-browser is provable in the gate, note it and move on

### Project Structure Notes

- NEW `src/components/chart-form-toggle.astro`; NEW `src/scripts/chart-form-toggle.ts`; NEW `src/scripts/results-motion-core.ts` (pure)
- NEW `tests/unit/results-motion-core.test.ts`, chart-toggle/pie contract tests; NEW or extended `tests/e2e/motion.spec.mjs`
- UPDATE `src/styles/tokens.css` (spark width, chart-form-toggle family, stale comment), `src/components/results-bar.astro` (motion CSS, tabular-nums, data attrs, header comment), `src/components/results-tally.astro` (toggle slot, totals-line split, `yourBallotOptionIds`), `src/scripts/results-live.ts` (arming, spark, count-up, snap contexts), `src/pages/[reference].astro` (prop wiring, grid rule, script load), `src/pages/[reference]/results.astro` (script load), `tests/e2e/live-results.spec.mjs` + `tests/e2e/results.spec.mjs` (snap-era assertion updates), `tests/unit/live-indicator.test.mjs` only if the region markup split requires it, `CHANGELOG.md`
- PRESERVE `src/scripts/results-live-core.ts` (transport core — consumed, not changed), `src/components/live-indicator.*` (the pulse is done), `src/modules/**` and `src/adapters/**` (zero server-side change), `db/migrations/*`, `wrangler.jsonc`, `worker-configuration.d.ts`, package manifests, `src/middleware.ts`
- Do not create parallel design/decision records; ratified-assumption outcomes and evidence belong in this story's Dev Agent Record

### References

- [Source: `AGENTS.md:102-127,176-234,245-265,282-287,338-339` — gate order, git flow, commits, CHANGELOG, story records, smoke-test token trap, no placeholders]
- [Source: `_bmad-output/planning-artifacts/epics.md:489-516` — Story 1.10 contract; `:92-117` UX-DR1/3/4/5/6/17/19/25; `:68-88` AR list (AR-7 `:74`, AR-20 `:87`, AR-21 `:88`); `:41` FR-21; `:56-57` NFR-5/6; `:157` sequencing rationale]
- [Source: `_bmad-output/planning-artifacts/architecture/architecture-oddspark-polls-2026-07-29/ARCHITECTURE-SPINE.md:56-64,169-178,180-192,279-288,378-379,409-438,484,513,520`]
- [Source: `_bmad-output/planning-artifacts/ux-designs/ux-oddspark-polls-2026-07-28/DESIGN.md:114-120,168-203,357-358,405,429,440,452,457,485,513,524-528,540,554-574,641,674,676,686-697` — motion tokens, results-bar/chart-form-toggle blocks, choreography, tie, zero-radius, Don't list]
- [Source: `.../ux-designs/.../EXPERIENCE.md:23,62,83,142-143,194-199,219-226,242,250-260,266-291,352,426`; `mockups/key-tally.html:113-143,229`; `review-accessibility.md:21,23,35,37,103`]
- [Source: `_bmad-output/planning-artifacts/implementation-readiness-report-2026-07-29.md:160,206` — FR-21 story mapping, toggle JS sanction; the `:216` lostTypography defect was resolved in Story 1.9]
- [Source: `_bmad-output/implementation-artifacts/1-9-live-updating-results.md` — full Dev Notes, Review Findings, File List; `deferred-work.md:5,9`]
- [Source: `src/scripts/results-live.ts:117,169-211,280-393,497-499,532-582`; `src/scripts/results-live-core.ts`; `src/components/results-bar.astro:9,44-74,93-104`; `src/components/results-bar.ts`; `src/components/results-tally.astro:55-171,205-207,247-254`; `src/components/live-indicator.astro:81-86`; `src/pages/[reference].astro:432-446,519-540,611-617,731-778,888-930,1107-1135`; `src/pages/[reference]/results.astro`; `src/styles/tokens.css:12-52,87-93,100-105,142-149,156-247`]
- [Source: `tests/e2e/live-results.spec.mjs:23-204,252-261,311-342,550-557,648-651`; `tests/e2e/results.spec.mjs:272-290,403-404,772-791,806-812,861-865,891-894,1109-1111`; `tests/unit/live-indicator.test.mjs:57-92`; `tests/unit/no-raw-html.test.mjs:13-22`; `tests/e2e/creator-session.mjs`; `playwright.config.ts`]

## Dev Agent Record

### Agent Model Used

Kimi Code CLI (Moonshot AI) — dev-story workflow, 2026-08-02

### Implementation Plan

Executed Tasks 1–6 in story order, red-green per task, narrow suites during each task and the repository gate at the end:

1. CSS foundation first (tokens + gated transitions + spark keyframes + reduced-motion block) so every later task wires into an already-proven contract.
2. Pure motion core (`results-motion-core.ts`) unit-tested on Node before the poller wiring that consumes it; same pattern for pie geometry (`chart-pie-core.ts`) before the renderer.
3. Toggle component + script + placement, then the pie renderer into the toggle's mount point, then the own-vote spark on the confirmation render.
4. Snap-era e2e assertions updated deliberately (each mapped to the AC that now demands motion), new `motion.spec.mjs` proofs, browser captures viewed and verified, CHANGELOG, full gate.

### Debug Log References

- Reduced-motion specificity bug (e2e): the Task 1 reduced-motion block used bare `.results-bar-fill` (0,2,0), which loses to the armed/spark selectors (0,5,0) — media queries add no specificity, so armed fills kept transitioning under `reduce`. Caught by the `live-results.spec.mjs` flip-back assertion; fixed by mirroring the exact armed/spark selectors inside the reduced block (later source order wins); verified in compiled CSS and e2e.
- Initial-paint guard vs double-rAF arming (e2e): with post-paint timer arming, the KEEP-mandated warm-load guard (`results.spec.mjs:861-865`) read armed transition values on an idle tally. Arming moved to lazy — applied in the same task as the first animated reconcile — so nothing can move before the first update, the guard passes unweakened, and the first in-cadence update still animates. Poller comment documents this.
- `route.abort()` in the stale-recovery test logs `ERR_FAILED` and fails the console collector — switched to the house malformed-200 `route.fulfill` idiom.
- Closed-between-vote-and-render test: Playwright does not re-invoke route handlers for redirect-chain hops (proven by a debug run), so the original close-during-redirect approach raced. Rebuilt deterministically: `page.request.post` with `maxRedirects: 0` (house `origin`/`sec-fetch-site` CSRF headers) → `closePoll` → fresh `page.goto`.
- `@cloudflare/vitest-pool-workers/types` globally merges HTMLRewriter's `Element.append(string | ReadableStream | Response)` into the DOM interface and shadows the DOM overload — `tsc` rejected every `.append(node)`. Matched the repo idiom (`create-poll-form.ts`) with `appendChild`.

### Completion Notes List

- All six ACs satisfied and proven end to end: armed width settle + spark + count-up (AC #1), coalescing to latest (AC #2), leader cross-fade with moving ◆ and tie withdrawal (AC #3), reduced-motion instant landing at full fidelity (AC #4), the `BARS · PIE` toggle with the static pie (AC #5), and the own-vote confirmation spark (AC #6).
- Ratified-assumption outcomes, for the record:
  - **Spark easing:** `ease-in-out` (symmetric 2→4→2 flash), as ratified.
  - **Toggle ARIA:** two native `<button type="button">` with `aria-pressed` in a `role="group" aria-label="Chart form"` — no third pattern invented.
  - **Multi-select suppression:** the toggle does not render on multi-select Polls — per-option percentages are shares of VOTERS and sum past 100%, so no honest pie exists and re-basing would make PIE disagree with BARS. Recorded here as mandated; CHANGELOG carries the user-facing note.
  - **Pie technique/colors/legend/data source:** inline SVG via `document.createElementNS` (no `innerHTML` — build-guarded); token-bound washes with a 1px `--color-rule` boundary stroke and `--color-solar-wash` leader slice; legend rows reuse `barAccessibleName` with `47% · 122` clusters and exactly one ◆ (withdrawn on tie); angles derive only from the exact server-computed `data-pie-share` while rounded `data-percent` remains display data (the AD-9/NFR-6 boundary sentence is the `chart-pie-core.ts` header comment). Every positive option retains a wedge; only floating-point closure adjusts the final slice. SVG is `aria-hidden`; persistent option-keyed legend rows carry the accessible content.
  - **Own-vote spark hook location:** `chart-form-toggle.ts` (ratified option A), called before the toggle's own early-return so multi-select confirmations (no toggle) and closed-between-vote-and-render confirmations (no poller) still spark; fires on a double rAF registered after the cold-load enhancer's reveal rAF, so the reveal lands first. Reduced motion omits it entirely.
- **Deviation 1 — Task 1 tokens:** no new raw `-dark`/`-light` source pairs were added for the toggle family; every color it names already has a mode-collapsed palette source, so the family follows the file's existing mode-bound-component-token precedent (declared in all four collapse blocks) instead of alias indirection. `typography: label-caps` likewise references the existing `--type-label-caps-*` tokens directly. The four-place collapse requirement is met and contract-tested.
- **Deviation 2 — spark ungated:** the `is-spark` animation rule is deliberately NOT gated behind `is-motion-armed` (only the transitions are). SSR never emits `is-spark`, so initial paint still cannot animate, and the own-vote spark works on surfaces where the poller/arming never loads (closed-Poll confirmations).
- **Deviation 3 — lazy arming (story-text deviation):** Task 2's ratified "double requestAnimationFrame after first paint" conflicted with the KEEP-mandated idle-warm-load `0s` guard; arming now happens in the same task as the first animated reconcile. Both executable contracts hold; the story text loses. Documented in the poller comment.
- **Deviation 4 — own-vote marker gated on `outcome?.code === "counted"`:** `castSelectionIds` also resolves on replayed/already-voted renders; emitting `data-your-option` there would spark on every revisit and cheapen the one sanctioned exception. The attribute is emitted only for the fresh flash-consumed confirmation. IDs and labels come from one shared filter pass, so they can never disagree.
- **Multi-select summary (`{n} VOTERS · {m} SELECTIONS`) left snapping, not tweened:** EXPERIENCE.md:277 binds the totals line, not the summary; leaving it avoids the SSR-line/poller-mirror desync risk entirely. The chatter-trap split was applied to the totals line only: `aria-hidden` visual span ticks while the visually-hidden announced span receives the final value exactly once per reconcile (1.9's single-announcement semantics, region persistence, single `aria-live`, and source order all preserved — `live-indicator.test.mjs` needed no changes).
- **Pie layout legibility:** the pie is the least-specified surface in the UX package (no mockup); the shipped composition is a 160px ring above the legend, verified in captures at 375px dark and 1280px light — reads cleanly in both. No design tokens were invented for it beyond the ratified slice-color bindings.
- The `1 VOTERS` pluralization finding stays deferred in `deferred-work.md:5` — untouched, no source TODO.
- Full code review resolved all 12 patch findings: exact PIE shares, atomic settles, explicit PIE/recovery/reduced-motion snaps, lifecycle-safe sparks, stable number columns, persistent legend rows, token-sourced easing, and deterministic browser proofs. Final gate: 655 Vitest tests and 99 Playwright tests, plus migration guard, TypeScript, generated-binding drift check, and production build.
- No migration, dependency, binding, or endpoint change. The server-owned Results view contract now supplies exact `pieShare`; adapters, middleware, `wrangler.jsonc`, and generated binding types remain unchanged.

### File List

**New:**
- `src/components/chart-form-toggle.astro`
- `src/scripts/chart-form-toggle.ts`
- `src/scripts/chart-form-contract.ts`
- `src/scripts/chart-pie-core.ts`
- `src/scripts/results-motion-core.ts`
- `src/scripts/results-spark.ts`
- `tests/e2e/motion.spec.mjs`
- `tests/unit/chart-form-toggle.test.mjs`
- `tests/unit/chart-pie-core.test.ts`
- `tests/unit/chart-pie.test.mjs`
- `tests/unit/own-vote-spark.test.mjs`
- `tests/unit/results-bar-motion.test.mjs`
- `tests/unit/results-motion-core.test.ts`

**Modified:**
- `src/styles/tokens.css` (spark width token, chart-form-toggle family, stale motion comment)
- `src/components/results-bar.astro` (gated motion CSS, spark keyframes, reduced-motion block, tabular-nums, `data-percent`/`data-count`/`data-your-option`, header comment)
- `src/components/results-tally.astro` (toggle + pie mount slot, totals-line aria split, `yourBallotOptionIds` prop, multi-select suppression)
- `src/modules/results/index.ts` (server-owned exact `pieShare` alongside rounded display percentage)
- `src/scripts/results-live.ts` (lazy arming, snap contexts, spark trigger, count-up tweens, chatter-trap totals split, dataset writes)
- `src/pages/[reference].astro` (toggle script load, desktop grid placement, `yourBallotOptionIds` wiring)
- `src/pages/[reference]/results.astro` (toggle script load, ungated by status)
- `tests/e2e/live-results.spec.mjs` (armed/reduced transition assertions, two-toggle-button allowance)
- `tests/e2e/results.spec.mjs` (no-leak literals extended)
- `tests/integration/live-results-route.integration.test.ts` (exact `pieShare` response contract)
- `tests/unit/results-live-contract.test.mjs` (motion wiring contracts, lazy arming)
- `tests/unit/results.test.ts` (exact-share, overshoot, and rounded-zero geometry cases)
- `CHANGELOG.md` (Unreleased: motion system + chart-form toggle)

## Change Log

| Date | Change |
|---|---|
| 2026-08-02 | Closed the full Story 1.10 code review: resolved all 12 patch findings, including exact server-owned PIE geometry, atomic reconciles, no-motion state transitions, spark cleanup, stable numeric layout, persistent legend rows, and expanded deterministic browser coverage. Final gate: `pnpm migrations:guard`, `pnpm test` (655), `pnpm check`, `pnpm test:e2e` (99), `pnpm types` + drift check, and `pnpm build:production` all green. |
| 2026-08-02 | Implemented Story 1.10 (Tasks 1–6): motion system (bar transition, spark, count-up, leader cross-fade, reduced-motion snap, coalescing, snap contexts), `BARS · PIE` chart-form toggle with static SVG pie + legend, own-vote confirmation spark; snap-era e2e assertions updated; browser proof captured; CHANGELOG updated. Gate: `pnpm migrations:guard`, `pnpm test` (641), `pnpm check`, `pnpm test:e2e` (94), `pnpm types` + drift check, `pnpm build:production` all green. |
