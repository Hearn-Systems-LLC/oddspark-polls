---
baseline_commit: de8430731bf8ef3c2325e421961a97abafe84a27
---

# Story 1.11: Creator Dashboard

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->
<!-- Branch provenance: cut `story/1-11-creator-dashboard` from origin/main at de8430731bf8ef3c2325e421961a97abafe84a27 (Story 1.10 merge). -->

## Story

As a Creator,
I want a dashboard listing my Polls with at-a-glance status and counts,
so that I can monitor everything I'm running from one place.

## Acceptance Criteria

1. **Given** a signed-in Creator at `/creator`, **When** the page renders, **Then** it lists only that Creator's Polls as `poll-card` rows — question title, caption metadata line (type · votes · closing time), live indicator on open Polls, `CLOSED` label-caps-lg on closed ones — newest first with live Polls above closed, each whole row one tap target to the Poll detail (UX-DR11).
2. **Given** a Creator with no Polls, **When** `/creator` renders, **Then** it shows "No Polls yet. The empty state, working as intended." plus the create action.
3. **Given** a Creator attempting to view or act on another Creator's Poll, **When** the request reaches the server, **Then** it is denied by ownership checks against the internal user ID — route hiding is never the authorization (NFR-3, AD-4).
4. **Given** a viewport at the `lg` breakpoint or wider, **When** the creator surface renders, **Then** it splits two-column — Poll list left, selected Poll detail right — while below `lg` it remains single-column with no component hidden or added (UX-DR25).

## Tasks / Subtasks

- [x] Task 1: `poll-card` tokens + component (AC: #1)
  - [x] UPDATE `src/styles/tokens.css`: add the `--poll-card-*` family from the DESIGN.md frontmatter block (DESIGN.md:310-318): `background: transparent`, `paddingY: {spacing.6}` (24px, `--space-6` at `:116`), `borderTop: {spacing.hairline} solid {colors.rule-dark}` (`--space-hairline`/`--color-rule`), `titleTypography: {typography.poll-question}` (Newsreader 26px/400/1.3 — `--type-poll-question-*` `:64-65`), `metaTypography: {typography.caption}` (Courier Prime 12px/400/1.5), `metaColor: {colors.dim-dark}`, `statusTypography: {typography.label-caps-lg}`, `statusColor: {colors.dim-dark}`. Every binding resolves to an already-mode-collapsed var (`--color-rule`, `--color-dim`), so ONE `:root` component block suffices — the `--results-bar-*` precedent at `tokens.css:143-151`, NOT the four-block `--chart-form-toggle-*` shape. NEVER delete or rename `--color-solar-dark` (`scripts/smoke.mjs` extracts it; a rename breaks the deploy gate by design — AGENTS.md:282-287)
  - [x] NEW `src/components/poll-card.astro`: a ROW, not a card — "Not a card. A row" (DESIGN.md:623); "No card grids. This is not a dashboard." (DESIGN.md:686 — verbatim, on the Don't list). Anatomy: top hairline separating neighbours (rules run edge to edge, never enclose — DESIGN.md:518); title in poll-question typography; caption metadata line in caption/dim (`MULTIPLE CHOICE · 122 VOTES · CLOSES IN 3H` — DESIGN.md:623, EXPERIENCE.md:165); open Polls carry `<LiveIndicator status="open" />`, closed carry `CLOSED` in label-caps-lg dim — never `faint` ("no `faint` on text a user has to read — including a closed-Poll status", DESIGN.md:689, `:438`). Whole row is ONE `<a>` tap target with title/caption/status inside it — no secondary actions inside the row, the live indicator is decorative and never interactive (EXPERIENCE.md:155); the dot is already `aria-hidden` (`live-indicator.astro:30`). `[ASSUMPTION: link mechanism]` block-level `<a>` wrapping the row content, per the poll-option/security-toggle "native element, content inside it" precedent (DESIGN.md:540,580); accessible name is the row's natural text content, decorative glyphs excluded. Props take one structured `PollCardViewModel` — keep the component dumb and compose the presentation model in component-adjacent `src/components/poll-card.ts`, leaving the Polls domain module presentation-free (AD-1)
  - [x] Focus/hover: `poll-card` deliberately has NO focus/hover tokens (DESIGN.md:310-318 vs `poll-option` `:295-307`) — the GLOBAL rules govern: 2px outline / 2px offset bound to `--color-focus-ring` (EXPERIENCE.md:222; "bind every focus outline to `focus-ring-*`, never to `solar-*`" — DESIGN.md:678). No hover invention — "the focus outline, not the hover fill, is what carries the affordance" (DESIGN.md:542); hover-only affordances are banned (EXPERIENCE.md:225). Tap target: creator surface is 44px minimum, NOT 48 (EXPERIENCE.md:219 — the 48px list is enumerated and voting-surface-only); 24px paddingY + a 26px title line clears it structurally
  - [x] Unit: NEW `tests/unit/poll-card.test.mjs` source-contract test (the `readFileSync` + token-name/regex idiom — `tests/unit/chart-form-toggle.test.mjs:1-14`): tokens bound by `var(--poll-card-*)` name, single `<a>` row target, no `set:html`, no interactive elements besides the link, hairline via borderTop token, CLOSED never styled `faint`

- [x] Task 2: `listPollsForOwner` query — adapter, presenter, labels (AC: #1, #3)
  - [x] NEW adapter method `listPollsForOwner(ownerUserId, nowMs)` on `createPollPersistence` (`src/adapters/d1/index.ts` — sibling of `findPollForOwner` at `:203-228`, whose `WHERE p.owner_user_id = ?2` filter is the ownership idiom to mirror). ONE owner-driven statement, no N+1: scan `poll` through `poll_owner_user_id_idx`, filter `p.owner_user_id = ?1`, and use a correlated `SELECT COUNT(*) FROM vote AS v INDEXED BY vote_poll_id_idx WHERE v.poll_id = p.id` for each owned row. This avoids materializing or grouping Votes for other creators while retaining zero counts. Integration evidence must assert `EXPLAIN QUERY PLAN` uses both indexes and has no `MATERIALIZE` step. Do NOT loop `projectVersionedTally` per row (it is per-poll, `:481-606`), and do NOT reuse `findPollForOwner` in a loop (it loads options per poll, `:223` — N+1). Do NOT join `poll_reference` — the card links by poll id, not reference, so the list query needs no reference at all (this also sidesteps the zero-canonical-row corrupt state deferred from 1.3 review round 2, `deferred-work.md:48`)
  - [x] Sort in SQL, effective state computed at request time — NEVER stored (AD-11, `ARCHITECTURE-SPINE.md:195-201`): `ORDER BY (p.closed_at_ms IS NOT NULL OR (p.deadline_ms IS NOT NULL AND p.deadline_ms <= ?now)) ASC, p.created_at_ms DESC`. `?now` is a bound parameter computed ONCE in the page frontmatter (`const nowMs = Date.now()` — the `[reference]/results.astro:50` pattern); no `Date.now()` inside domain or adapter code (AR-19). The same `nowMs` feeds `effectivePollStatus` (`src/shared/domain/index.ts:40-51`) for each row's LIVE/CLOSED render so SQL ordering and rendered status can never disagree
  - [x] `[ASSUMPTION: votes = distinct accepted Vote rows — RATIFIED]` the caption's "votes" number is `COUNT(*) FROM vote WHERE poll_id = …` — the same number `results-tally` announces, rendered through the existing `formatVoteTotal(voterCount)` helper (`src/components/live-indicator.ts:14-16` — `1 VOTE`/`122 VOTES`, singular handled). On multi-select Polls this is voters, not selections — selections summing past the voter count is a Tally concern (UX-DR3), not a dashboard one. Never a per-option or selection count here
  - [x] Presentation composition as one pure, component-adjacent model builder in `src/components/poll-card.ts` (AD-1: the Polls domain owns lifecycle/configuration facts, while labels and datetime copy belong to the presentation layer): NEW exhaustive `POLL_TYPE_LABELS` map (`multiple_choice` → `MULTIPLE CHOICE`; cover all four `PollType` members, `src/shared/domain/index.ts:12-18`) + `buildPollCardViewModel()`, returning structured type, vote-total, and discriminated closing segments rather than a formatted string that the component must reparse. `[ASSUMPTION: closing-time segment — RATIFIED]` open + deadline under 24h: `CLOSES IN {n}H`/`{n}M` via the existing domain helper `countdownLabel(deadlineMs, nowMs)` (`src/modules/polls/deadline-display.ts:5-17`); open + deadline ≥24h: an absolute segment carrying `deadlineMs` and the server-rendered `formatUtc` floor (`src/lib/datetime.ts:4-13`) for `time[data-deadline]`, localized by the existing `src/scripts/deadline-time.ts` enhancement — the no-JS floor stays complete; open + no deadline: the segment is omitted (two-segment caption, no hole in the line); closed: segment omitted — `CLOSED` carries the state, a stale "closes" time would lie. The component renders literal ` · ` separators directly
  - [x] Integration: NEW `tests/integration/creator-dashboard-adapter.integration.test.ts` (house setup: `applyD1Migrations` + explicit truncation order + `INSERT OR IGNORE` owner rows + frozen `const NOW`, per `polls-adapter.integration.test.ts:1-42` / `results-adapter.integration.test.ts:28-39`): seed TWO owners and prove the ownership filter returns only owner A's polls (AC #3's query-level teeth); vote counts correct (0, 1, many; multi-select counts voters not selections); ordering — live above closed with newest-first inside each group; a deadline-passed-but-unmaterialized poll (`closed_at_ms` NULL, `deadline_ms < now`) sorts and reports as CLOSED (AD-11's request-time rule is exactly this case)
  - [x] Unit: `POLL_TYPE_LABELS` covers every `PollType` member (exhaustiveness-checked); `buildPollCardViewModel` branch coverage (all four closing-segment cases, singular vote, status, and navigation state)

- [x] Task 3: `/creator` — replace the placeholder with the dashboard (AC: #1, #2, #3)
  - [x] REWRITE `src/pages/creator/index.astro` (currently a 5-line placeholder rendering `CreatorPlaceholder`). Follow the established creator-page frontmatter recipe from `creator/polls/[pollId].astro`: (1) `Astro.response.headers.set("cache-control", "private, no-store")` FIRST (AD-21 — the dashboard is per-creator private data carrying vote counts; `private, no-store` on every response including early returns); (2) 405 method gate; (3) `const principal = Astro.locals.requestContext?.principal ?? null` with a HAND-BUILT 303 to `/sign-in?return=…` on null — `Astro.redirect()` drops `Astro.response.headers`, so redirects are built as `new Response(null, {status: 303, headers: {location, "cache-control"}})` (`creator/polls/[pollId].astro:20-31`, comment at `:22-23` is load-bearing); defense-in-depth behind `creatorGuardMiddleware` (`src/middleware.ts:230-255`), which already 303s unauthenticated `/creator/*`. Read principal from `Astro.locals.requestContext.principal` (the creator-page convention — `creator/new.astro:40-41`), ownership key `principal.userId as UserId`
  - [x] PRESERVE the sign-in landing flash: `creator-placeholder.astro:4,12-17,27-30` handles `?outcome=signed-in` with an autofocused outcome line, and `createSignInDestinations` targets `/creator?outcome=signed-in` (`src/modules/identity/index.ts:75-79,91`). The dashboard must keep the post-submit contract on that arrival: outcome line first in the main landmark, `tabindex="-1"`, focused on load, `<title>` leading with the outcome (UX-DR17). `creator-placeholder.astro` becomes unused ONLY if `creator/[...path].astro` also stops using it — the catch-all placeholder stays this story (it is `/creator/*` 404-shaped territory beyond this story's scope); delete `creator-placeholder.astro` only if nothing imports it after the rewrite
  - [x] Render: page heading, then the poll list — `poll-card` rows linking to `/creator/polls/{poll.id}` (the existing ownership-checked detail route — internal id, not reference). Server-rendered snapshot, NO client polling: AD-10's conditional polling is specified for one per-Poll versioned result projection (`ARCHITECTURE-SPINE.md:180-192`) — nothing extends it to a list, a list version endpoint would be a NEW endpoint (prohibited without a new decision), and UX-DR25's sanctioned hand-written-JS list is voter-surface only (epics.md:116). Consequence, ratify visibly: the per-card live indicator renders its pulsing dot (many dots may pulse; reduced-motion holds them all — `live-indicator.astro:81-86`) but the lost-connection replacement semantics (EXPERIENCE.md:158) do NOT apply — there is no subscription on this surface. Counts are as-of-page-load
  - [x] Empty state (AC #2): the copy verbatim — "No Polls yet. The empty state, working as intended." (EXPERIENCE.md:109; layout spec is just "Empty-state line plus the create action", EXPERIENCE.md:204) — plus `[ASSUMPTION: create action — RATIFIED]` `<ButtonPrimary href="/creator/new">CREATE A POLL</ButtonPrimary>` (`button-primary.astro:25-35` renders `<a>` when given `href`; nothing else competes on `/creator`, so the one-primary-per-screen rule is satisfied — DESIGN.md:601). No exclamation marks, no emoji (EXPERIENCE.md:73). `[ASSUMPTION: create entry on the non-empty list — RATIFIED]` the create action renders on the populated dashboard too (a dashboard you can only create from when empty is a dead end; UJ-1 starts at "she creates a Poll from `/creator`") — same control, above the list, still the screen's only primary
  - [x] No skeleton state: skeleton `poll-card` rows are specified for Discover only (EXPERIENCE.md:210); `/creator` is fully server-rendered so no loading state is reachable. No pagination `[ASSUMPTION — RATIFIED]`: pagination is specified for Discover only (~20/page, EXPERIENCE.md:166; DESIGN.md:625); the creator list renders ALL of the creator's own polls — bounded by their own creation rate, and "nothing is archived away" (EXPERIENCE.md:155) forbids hiding closed rows. Infinite scroll is banned outright (EXPERIENCE.md:225). Revisit only if a real creator's list makes the page heavy
  - [x] Escaping: question titles are creator-supplied untrusted input — Astro's default escaping only, no `set:html` anywhere (`tests/unit/no-raw-html.test.mjs:13-22` fails the build otherwise; epics.md:303)
  - [x] Telemetry: automatic via middleware; leave `requestContext.pollId` null — a list has no single poll (`[reference]/results.astro:84-86` sets it only when one Poll resolves). Never log digests or tokens (AD-15)

- [x] Task 4: Creator surface two-column at `lg` + minimal detail growth (AC: #4)
  - [x] `[ASSUMPTION: what "selected" means — RATIFIED]` selection is NAVIGATION, not client state: the IA gives Poll detail its own route reached from the list row (EXPERIENCE.md:49,62,155 — "one tap target leading to Poll detail"). The creator surface with a selection IS `/creator/polls/{id}`; `/creator` is the surface with no selection. Therefore: UPDATE `src/pages/creator/polls/[pollId].astro` (the create-confirmation page, whose own header comment says it "grows into the full poll detail with Stories 1.11/1.12") to render the creator's poll LIST alongside the detail — list left, detail right at ≥1024px; below `lg`, single column with BOTH regions present, stacked in DOM order (list, then detail), exactly how the post-vote Tally's two columns stack on mobile (DESIGN.md:507 — "the creator surface splits the same way"; EXPERIENCE.md:234,236). Nothing hidden, nothing added across the breakpoint (UX-DR25). On `/creator` (no selection) there is no detail region — an unselected surface has no selected detail; that is absence of state, not a hidden component
  - [x] Grid technique: the post-vote precedent at `src/pages/[reference].astro:1092-1115` — `@media (min-width: 1024px)` with `display: grid; grid-template-columns: …; column-gap: var(--space-8); align-items: start`, explicit `grid-column`/`grid-row` placement per region. Media queries use LITERAL `1024px` — custom properties cannot appear in media queries (`tokens.css:96` comment; `--bp-lg` is reference-only). `[ASSUMPTION: column geometry — RATIFIED]` list column 320px / detail `1fr` (mirroring the post-vote `320px 1fr`); the UX package specifies no widths, ratio, gap, or scroll ownership — one page scroll, no sticky, no independent panes. Margins/measure per global rules: 20px mobile / 48px desktop, 68ch (DESIGN.md:497,503)
  - [x] `[ASSUMPTION: selected-row treatment — RATIFIED]` the selected poll's row in the left list carries `aria-current="page"` and NOTHING visual beyond it — no token, rule, or invented treatment exists for selection (the word "selected" is never applied to `poll-card` in the UX package), and "add space before you add a rule, and add a rule before you ever add a box" (DESIGN.md:512) counsels restraint; the detail pane's presence carries the selection. Record any deviation in the Dev Agent Record
  - [x] Detail region content this story (the artifacts are SILENT on 1.11's detail pane — everything named for Poll detail arrives in 1.12/Epic 2/Epic 3): KEEP the existing confirmation content (question `<h1>`, description, POLL LINK block `creator/polls/[pollId].astro:48-50,67-96`, deadline echo, `?created` flash) and ADD the monitor floor: the status line — `<LiveIndicator status={…}>` with the vote total in its metadata slot, slot idiom at `results-tally.astro:101-111` — and a `VIEW LIVE RESULTS` `button-secondary` link to `/{canonicalReference}/results` `[ASSUMPTION — RATIFIED]`: both journey climaxes define monitoring as opening the live Tally from the dashboard (EXPERIENCE.md:337,403 UJ-1/UJ-6), and the owner is entitled there under every Visibility Setting via the existing `ViewerContext` authorization (Story 1.8) — LINK to the live surface, never embed `results-tally` or plumb a second results projection into a creator page. Do NOT add Share here (share-action is scoped to create-confirmation/voting/Tally — DESIGN.md:627; putting it on the detail pane would be an addition to the package, not a reading of it). Do NOT add close/edit/delete (1.12), toggles (2.1), listing control (3.1), export (4.x)
  - [x] Status derivation on the detail page: `effectivePollStatus({closedAtMs, deadlineMs}, nowMs)` with one request-scoped `nowMs` — `findPollForOwner` already returns the lifecycle fields (`OwnedPoll`, `src/adapters/d1/index.ts:47-51`); the vote count for the slot needs the count from Task 2's query path (fetch the creator's list on this page anyway — the left column needs it; pull the selected row's count from that same result, one query, no second projection)
  - [x] Unit: extend the metadata/status pure-function tests for the detail status line; source-contract test asserting `aria-current` wiring, the 1024px grid block, and that no share-action/overlay/toggle markup exists on the detail page this story

- [x] Task 5: Prove it end to end, close the gate (AC: all)
  - [x] NEW `tests/e2e/creator-dashboard.spec.mjs` with the house idioms: serial single worker over shared local D1 (`playwright.config.ts` — chromium only, port 4391, `workers: 1` is deliberate), `test.skip(!hasBetterAuthSecret(), …)` guard, seeded sessions via `seedCreatorSession()` + `context.addCookies` (`tests/e2e/creator-session.mjs:104-139`; consumer pattern `create-poll-authed.spec.mjs:20-48`), `afterAll` → `cleanupCreator(userId)` per seeded user, `requireBaseUrl(baseURL)`, `watchConsole` (any console error fails)
  - [x] Ordering/status proofs: seed polls and use the fixture mutators built for exactly this — `agePoll` backdates `created_at_ms` (newest-first proof), `closePoll` + `setPollDeadline` (live-above-closed grouping; CLOSED label; a deadline-passed-unmaterialized poll renders CLOSED), `tests/e2e/creator-session.mjs:157-224`. Assert the caption literal shape (`MULTIPLE CHOICE · N VOTES · …`), singular `1 VOTE`, and the no-deadline two-segment caption
  - [x] Ownership proof (AC #3): seed TWO creators; creator B's dashboard never lists creator A's polls; creator B requesting `/creator/polls/{A's id}` gets the existing 404 shape (404-not-403 is the established non-enumeration behavior, `creator/polls/[pollId].astro:38-40`) — new TEST, not new code
  - [x] Empty state proof (AC #2): fresh creator sees the exact copy byte-for-byte and the create action links `/creator/new`; signed-out `/creator` still 303s to `/sign-in` with a return address (middleware behavior — assert it stands)
  - [x] Two-column proof (AC #4): at 1280px viewport, `/creator/polls/{id}` shows list left / detail right (grid computed styles); at 375px both regions present single-column, DOM order stable; `aria-current="page"` on the selected row; whole-row link navigates; focus visible on rows (2px ring)
  - [x] Signed-in flash proof: `/creator?outcome=signed-in` renders the outcome line focused, `<title>` leads with the outcome
  - [x] No-JS floor: with JavaScript disabled the dashboard renders complete — rows, captions, statuses, UTC deadline floor, working links (server-rendered list; `deadline-time.ts` is enhancement only)
  - [x] Browser proof is mandatory (AGENTS.md:121-124 — don't ask Justin to eyeball it): captures at 375px dark and ≥1024px light of the populated dashboard, the empty state, and the two-column detail surface; console clean throughout
  - [x] UPDATE `CHANGELOG.md` under `## [Unreleased]` (user-facing: creator dashboard). Deferred findings go to `deferred-work.md`, never a source TODO (AGENTS.md:338-339)
- [x] Run narrow red/green tests during each task, then the repository gate in documented order (AGENTS.md:113-118): `pnpm migrations:guard`, `pnpm test`, `pnpm check`, `pnpm test:e2e`, `pnpm types`, `git diff --exit-code worker-configuration.d.ts`, `pnpm build:production`. NO migration, dependency, binding, or endpoint change is expected in this story — the drift assertion stays in the gate

### Review Findings

- [x] [Review][Patch] Replace the platform-wide Vote aggregation with an owner-driven indexed count and amend Task 2's literal query prescription [src/adapters/d1/index.ts:255]
- [x] [Review][Patch] Move Poll-card view composition into the presentation layer and amend the story to preserve AD-1 [src/modules/polls/poll-card-meta.ts:9]
- [x] [Review][Patch] Restore the post-create outcome as the first content in the main landmark [src/pages/creator/polls/[pollId].astro:115]
- [x] [Review][Patch] Give the detail-list create action the required 44px minimum target [src/pages/creator/polls/[pollId].astro:123]
- [x] [Review][Patch] Cap the desktop detail pane's readable copy at the required 68ch measure [src/pages/creator/polls/[pollId].astro:326]
- [x] [Review][Patch] Scope the intentional 404 console suppression to the expected foreign-detail navigation [tests/e2e/creator-dashboard.spec.mjs:38]
- [x] [Review][Patch] Add semantic list/item structure around both Poll-card collections [src/pages/creator/index.astro:99]
- [x] [Review][Patch] Produce one structured Poll-card metadata model instead of formatting twice and regex-parsing copy [src/components/poll-card.astro:39]
- [x] [Review][Patch] Complete the promised redirect-status, closed-order, grid-placement, focus-ring, no-JS UTC deadline, and desktop empty-state E2E proofs [tests/e2e/creator-dashboard.spec.mjs:115]
- [x] [Review][Patch] Complete the promised detail status and overlay/toggle source-contract assertions [tests/unit/poll-card.test.mjs:71]

## Dev Notes

### Product and UX Contract

- UX-DR11 verbatim (epics.md:102): `poll-card` row pattern (Newsreader title, caption metadata line, hairline separation, whole row one tap target) reused on the creator list, landing page, and `/discover` — build the component reusable from day one; Story 3.2's Discover consumes it unchanged (DESIGN.md:625)
- `poll-card` prose spec (DESIGN.md:621-625): "Not a card. A row: title in `{typography.poll-question}`, a `{typography.caption}` `{colors.dim-dark}` metadata line (`MULTIPLE CHOICE · 122 VOTES · CLOSES IN 3H`), separated from its neighbours by a top hairline. Live Polls carry the live indicator; closed Polls carry `CLOSED` in `{typography.label-caps-lg}` `{colors.dim-dark}`" — `faint` at 2.05:1 is barred from closed status by name (DESIGN.md:438,689)
- Behavioral contract is ONE table row (EXPERIENCE.md:155): whole row one tap target to Poll detail; NO secondary actions inside the row; live indicator decorative, never interactive; "Sorted newest-created first with live Polls above closed ones `[ASSUMPTION: the sources don't specify an order]`" — the sort is the UX package's own flagged assumption, restated here as this story's decision; "Closed Polls carry `CLOSED` and stay in the list — nothing is archived away" (an anti-requirement: no archive tab, no filter hiding closed rows)
- Live indicator on cards is SPECIFIED present (EXPERIENCE.md:158 lists "Poll card" as a surface; DESIGN.md:623,625), 6px solar-ink dot + `LIVE` label-caps, pulse 2400ms, reduced-motion holds full opacity (DESIGN.md:639-643). The package never examined N dots pulsing in a list (review-accessibility.md:103 cleared the singular case only) — reduced-motion holds them all, and the dashboard's snapshot nature caps the exposure; note any legibility concern in the Dev Agent Record rather than inventing a static variant
- Creator surface split (DESIGN.md:507; EXPERIENCE.md:236): "the creator surface (Poll list left, selected Poll detail right)" — one of exactly two two-column surfaces; below `lg` "same single column, wider margins, longer measure. No structural change" (EXPERIENCE.md:234); "scaling by widening rather than rearranging… no layout gains a component at a larger breakpoint, and nothing is hidden at a smaller one" (DESIGN.md:505)
- Empty state copy verbatim (EXPERIENCE.md:109): "**No Polls yet.** The empty state, working as intended." Layout: "Empty-state line plus the create action." (EXPERIENCE.md:204). Copy is layout-neutral — never says where things are (EXPERIENCE.md:77); no exclamation marks, no emoji, no "Oops!" (EXPERIENCE.md:73)
- Accessibility floor: 44px targets on the creator surface (48px is voting-surface-only — EXPERIENCE.md:219), rows are targets edge to edge; 2px/2px focus ring via `focus-ring-*` everywhere (EXPERIENCE.md:222; DESIGN.md:678); focus order follows reading order (EXPERIENCE.md:249); every action keyboard-completable (EXPERIENCE.md:248); no keyboard shortcuts (EXPERIENCE.md:221); no icon-only controls (EXPERIENCE.md:261); state never color alone
- Don't list binding this story (DESIGN.md:684-698): no card grids ("This is not a dashboard."), no rounded corners (dot + Turnstile are the only exceptions), no shadows/gradients, no `faint` on required text, no opacity as state, no second elevation step, no bold Newsreader; banned everywhere: infinite scroll, toasts, hover-only affordances, spinners (EXPERIENCE.md:225); whitespace before rules, rules before boxes, hairlines never enclose (DESIGN.md:512,518)
- Delisted notice (EXPERIENCE.md:128,213) is Epic 3 — no listing-badge on cards this story

### Architecture Guardrails

- Module ownership: `polls` owns Poll lifecycle/configuration (AD-19, ARCHITECTURE-SPINE.md:290-301,477-484); there is NO capability-map row for the dashboard — it is a delivery surface over the polls row guarded by identity's creator middleware (SPINE:502-503). No named list query exists anywhere; this story names it (`listPollsForOwner`) under the polls module + D1 adapter, per the structural seed (SPINE:409-438)
- Authorization: NFR-3 verbatim — creator-surface actions scoped server-side to owned Polls against the INTERNAL user ID, never OAuth identifiers (prd.md:307; AD-4 SPINE:80-92); FR-1 gates VIEW, not just mutation (prd.md:91); "route hiding is never authorization" (SPINE:383). The ownership filter lives in the SQL (`WHERE p.owner_user_id = ?`), not in route logic
- AD-21/AR-17: `/creator` and `/creator/polls/{id}` serve `private, no-store` — the enumerated rule covers result/export queries (SPINE:318-328) and the dashboard carries vote counts, so the same posture applies; every creator surface already sets it (6 occurrences in `[reference].astro`, 5 in `creator/new.astro`). Set it first; repeat on every early return; hand-build redirects (Astro.redirect drops response headers)
- AD-11/AR-9: effective state derived at request time from `closed_at_ms`/`deadline_ms` — never stored, never scheduled (SPINE:195-201); the DB trigger mirrors it (`0006_votes.sql:40-56`); `effectivePollStatus` (`src/shared/domain/index.ts:40-51`) is the ONE helper — SQL ordering must encode the identical predicate with the same `nowMs`
- AD-23: `PollType`/`PollStatus` come from `shared/domain` enums (SPINE:342-353) — the caption's type label maps FROM `PollType`, never local string unions
- AD-2/NFR-5: server-rendered HTML, zero client JS by default (SPINE:56-64) — the dashboard needs NO new client script; `deadline-time.ts` reuse is an existing enhancement over a complete server floor. The sanctioned hand-written-JS list (UX-DR25, epics.md:116) is voter-surface only — do not add dashboard polling, sorting, or filtering scripts
- AD-9/AR-7: counts server-computed by SQL projection (SPINE:171-178) — the client never derives a number
- Explicit prohibitions: NO new endpoint (a list-version poller would need a new architecture decision); no D1 read replication, no Durable Objects, no discovery features (AR-21, SPINE:517-528); no new dependency (AD-18); read-only story — no `representation_version` increment, no migration, no binding change; no CSRF token needed (GET-only surface; `csrfMiddleware` exempts safe methods, `src/middleware.ts:194-199`) and UX-DR11's no-secondary-actions rule keeps mutations off the rows
- Voter privacy: nothing IP/session-derived ever reaches the Creator (NFR-4, prd.md:308) — the dashboard shows counts, never voter identities or digests
- AR-19 conventions: kebab-case files, branded IDs at boundaries (`principal.userId as UserId`), UTC ms in D1 (`0004_polls.sql:3-4` — auth tables are ISO TEXT, do not mix), one structured log per operation (automatic via middleware)
- AR-20/AGENTS.md:139-168: pure logic in `tests/unit` (Node), anything touching D1/Better Auth/middleware in `tests/integration` (workerd, `*.integration.test.ts`), journeys in `tests/e2e`; every new function gets a test in the same commit; test names read as prose

### Previous Story Intelligence (1.10, in review — and the 1.2–1.9 floor)

- 1.10 is UNMERGED at story creation: modified `tokens.css`, `results-bar.astro`, `results-tally.astro`, both `[reference]` pages, `results-live.ts`, two e2e specs + contract test; new chart-toggle/pie/motion files. 1.11's only shared file is `tokens.css` (disjoint token-family insertions). If 1.10's review mutates `results-tally.astro`'s LiveIndicator slot idiom, re-verify the Task 4 slot reference before copying it
- 1.10 lessons that transfer: source-contract unit tests are the enforcement mechanism for token binding (its `chart-form-toggle.test.mjs` shape); ratified-assumption outcomes and deviations belong in the Dev Agent Record, visibly; e2e `route.abort()` logs console errors that fail the collector — use the malformed-200 `route.fulfill` idiom if needed; `@cloudflare/vitest-pool-workers/types` shadows DOM `Element.append` — use `appendChild` in any DOM-touching TS (not expected this story)
- The component 1.9 built FOR this story: `live-indicator.astro` — props `{status: PollStatus, class?}`, dot only when open, default slot for metadata BEFORE the LIVE/CLOSED word, `CLOSED` steps up to label-caps-lg (`live-indicator.astro:10-16,29-35,53-55,81-86`); helpers `liveIndicatorState`/`formatVoteTotal` (`live-indicator.ts:8-16`); slot usage precedent `results-tally.astro:101-111`; built "specifically for 1.11 reuse" (`1-9-live-updating-results.md:51,102`)
- Creator-page recipe (1.2/1.3/1.4 floor): cache header first → method gate → principal from `Astro.locals.requestContext.principal` → hand-built 303s → ownership-scoped adapter call → 404 on null (`creator/polls/[pollId].astro:10-40`); middleware chain order is load-bearing: requestContext → telemetry → session → csrf → creatorGuard (`src/middleware.ts:324-330`; AGENTS.md:300-303); session lookup failures degrade to signed-out, never 500 (`:134-141`; AGENTS.md:315-324); `validateReturnAddress` requires a creator-surface relative path, falls back `/creator` (`src/modules/identity/index.ts:40-68`)
- Known deferred items that brush this story (do NOT fix silently): the unauthenticated-303 has no cache-control (middleware layer, deferred from 1.3 review); zero-canonical-reference corrupt state 404s `findPollForOwner` (deferred from 1.3 — Task 2's no-reference-join list query sidesteps it for the dashboard); `1 VOTERS` pluralization on the multi-select Tally summary stays deferred (`deferred-work.md:5`) — `formatVoteTotal` pluralizes correctly and is the helper this story uses
- E2E house facts: seeded-session harness forges the Better Auth cookie (better-call@1.3.7 signing — a better-auth bump breaking it is harness drift, `creator-session.mjs:6-12`); suites SKIP without `BETTER_AUTH_SECRET` in `.dev.vars` (the deploy gate provisions it — accepted idiom); every SQL-interpolated value passes `assertUuid`; `agePoll`/`closePoll`/`setPollDeadline`/`setResultVisibility`/`deletePoll`/`cleanupCreator` are the fixture levers (`creator-session.mjs:141-224`)

### Current Technical Information

- Pinned stack, no changes and NO new dependencies: Astro 7.1.5 + `@astrojs/cloudflare` 14.1.6, Better Auth 1.6.25, Zod 4.4.3, TypeScript 7.0.2, Vitest 4.1.10 + vitest-pool-workers, Playwright 1.62.0 (chromium only), Wrangler 4.115.0, Node 24.18.0, pnpm 11.17.0. Everything this story needs is baseline platform: one SQL query, one Astro page rewrite, one component, CSS grid at a literal 1024px media query
- Schema facts (all shipped): `poll(owner_user_id, poll_type, question, deadline_ms, closed_at_ms, created_at_ms, multi_select_enabled, …)` (`0004_polls.sql:11-26`, `0008_multi_select.sql:6-8`); `vote(poll_id, …)` + `vote_poll_id_idx` (`0006_votes.sql:5-16`); forward-only migrations, none expected this story

### Project Structure Notes

- NEW `src/components/poll-card.astro` + component-adjacent `src/components/poll-card.ts` presenter; NEW `listPollsForOwner` in `src/adapters/d1/index.ts`; NEW `tests/unit/poll-card.test.mjs`, presenter/label unit tests, `tests/integration/creator-dashboard-adapter.integration.test.ts`, `tests/e2e/creator-dashboard.spec.mjs`
- REWRITE `src/pages/creator/index.astro` (placeholder → dashboard). UPDATE `src/styles/tokens.css` (`--poll-card-*` family), `src/pages/creator/polls/[pollId].astro` (list column + status line + results link), `CHANGELOG.md`
- PRESERVE `src/pages/creator/[...path].astro` (catch-all placeholder — out of scope; an e2e guards it not shadowing `/creator/new`), `src/middleware.ts` (guard already correct), `src/modules/voting/**`, `src/modules/results/**` (no server-side results change), `db/migrations/*`, `wrangler.jsonc`, `worker-configuration.d.ts`, package manifests, everything on 1.10's file list (`results-bar/tally`, `results-live.ts`, chart/motion files)
- Do not create parallel design/decision records; ratified-assumption outcomes and evidence belong in this story's Dev Agent Record

### References

- [Source: `_bmad-output/planning-artifacts/epics.md:518-541` — Story 1.11 contract; `:102` UX-DR11; `:108` UX-DR17; `:116` UX-DR25; `:154-157` Epic 1 notes; `:68-88` AR list (AR-9 `:76`, AR-11 `:78`, AR-17 `:84`, AR-19 `:86`, AR-20 `:87`); `:303` creator-text escaping]
- [Source: `_bmad-output/planning-artifacts/architecture/architecture-oddspark-polls-2026-07-29/ARCHITECTURE-SPINE.md:46-64,80-92,109-118,171-178,180-192,195-201,220-230,290-301,318-328,342-353,370-383,409-438,477-484,502-503,517-528`]
- [Source: `_bmad-output/planning-artifacts/prds/prd-oddspark-polls-2026-07-28/prd.md:46,82,91,307-310,319-321`]
- [Source: `_bmad-output/planning-artifacts/ux-designs/ux-oddspark-polls-2026-07-28/DESIGN.md:310-318,437-438,497-518,526,540-542,580,601,621-625,631-633,639-643,678,684-698`; `EXPERIENCE.md:47-62,73-77,109,128,147,152-166,204-213,219-225,234-238,248-261,337,403`; `review-accessibility.md:19,103`; mockups: no creator/poll-card mockup exists — nearest precedent is `key-tally.html`'s two-column split]
- [Source: `AGENTS.md:102-127,139-168,174-233,236-265,282-287,300-324,338-339`]
- [Source: `_bmad-output/implementation-artifacts/1-9-live-updating-results.md:51,102`; `1-10-motion-system-chart-toggle.md` Dev Agent Record; `1-6-voting-page-states-resilience.md:28`; `deferred-work.md:5,44,48,60`]
- [Source: `src/middleware.ts:114-189,191-228,230-255,324-330`; `src/modules/identity/index.ts:1,35-38,40-68,75-91,124-126`; `src/modules/polls/reserved-slugs.ts:7`; `src/modules/polls/deadline-display.ts:5-17`; `src/shared/domain/index.ts:10-18,30-51`; `src/adapters/d1/index.ts:47-51,99,169,191,203-228,481-606,610-618`; `src/lib/datetime.ts:4-13`; `src/scripts/deadline-time.ts:33,56-60`]
- [Source: `src/pages/creator/index.astro:1-5`; `src/pages/creator/[...path].astro`; `src/pages/creator/new.astro:25-54,197-210,244`; `src/pages/creator/polls/[pollId].astro:8-96,133-141,158`; `src/components/creator-placeholder.astro:4,12-30`; `src/components/live-indicator.astro:10-16,29-35,53-55,81-86`; `src/components/live-indicator.ts:8-16`; `src/components/results-tally.astro:101-111`; `src/components/button-primary.astro:25-35`; `src/layouts/base-layout.astro`; `src/pages/[reference].astro:788-794,1092-1115`; `src/pages/[reference]/results.astro:49-50,84-86`; `src/styles/tokens.css:64-65,84-85,96-99,108-126,143-151,295-387`]
- [Source: `db/migrations/0004_polls.sql:3-4,11-28,43-50`; `0005_poll_reference_canonical_unique.sql:4-6`; `0006_votes.sql:5-24,40-56`; `0008_multi_select.sql:6-8`]
- [Source: `tests/unit/no-raw-html.test.mjs:13-22`; `tests/unit/chart-form-toggle.test.mjs:1-14`; `tests/integration/polls-adapter.integration.test.ts:1-42`; `tests/integration/results-adapter.integration.test.ts:28-39`; `vitest.integration.config.ts:7,37-47`; `tests/e2e/creator-session.mjs:6-12,25-31,39-43,74-102,104-155,157-224`; `tests/e2e/create-poll-authed.spec.mjs:20-48,57-58`; `playwright.config.ts:3-12`]

## Dev Agent Record

### Agent Model Used

Grok 4.5 (xAI) via bmad-dev-story; Codex (GPT-5) via bmad-code-review

### Debug Log References

- E2E ownership proof initially failed `watchConsole` on the intentional 404 navigation (`Failed to load resource: … 404`); review narrowed suppression to the exact registered foreign-detail document URL so unrelated 404s still fail.
- Review gate: migration guard passed; full unit + integration 681/681; full Playwright 108/108 (creator dashboard 9/9); `pnpm check`, `pnpm types`, worker-configuration drift, and `pnpm build:production` all green.

### Completion Notes List

- Implemented creator dashboard (Story 1.11): `--poll-card-*` tokens (single `:root` block), dumb `poll-card.astro` row link, component-adjacent `buildPollCardViewModel` / `POLL_TYPE_LABELS` presenter, `listPollsForOwner` (owner-indexed SQL + per-owned-poll indexed Vote count, no N+1, no reference join), rewrite of `/creator`, two-column `/creator/polls/{id}` at 1024px with list + monitor floor (`LiveIndicator` + vote total + VIEW LIVE RESULTS).
- Code review resolved both architecture decisions and all ten patch findings: owner-driven query planning, presentation-layer composition, semantic lists, accessible layout corrections, exact console filtering, and complete source/browser proof.
- Ratified assumptions followed: selection = navigation; selected row = `aria-current="page"` only (no invented visual); votes = distinct vote rows; create action on empty and populated lists; no pagination; no share/close/edit this story; no client list polling (snapshot counts, many LIVE dots may pulse).
- Absolute deadline captions use `time[data-deadline]` + optional `creator-dashboard.ts` → `enhanceDeadlineTimes` when any absolute segment is present.
- Browser proof screenshots under `test-results/creator-dashboard-proof/` (empty, list, and detail at 375px dark and 1280px light); all six were visually inspected after the review patches.
- `creator-placeholder.astro` retained for `creator/[...path].astro` catch-all only.
- No migrations, dependencies, bindings, or new endpoints.

### File List

- `CHANGELOG.md`
- `_bmad-output/implementation-artifacts/1-11-creator-dashboard.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `src/adapters/d1/index.ts`
- `src/components/poll-card.astro`
- `src/components/poll-card.ts`
- `src/pages/creator/index.astro`
- `src/pages/creator/polls/[pollId].astro`
- `src/scripts/creator-dashboard.ts`
- `src/styles/tokens.css`
- `tests/e2e/creator-dashboard.spec.mjs`
- `tests/integration/creator-dashboard-adapter.integration.test.ts`
- `tests/unit/poll-card-meta.test.ts`
- `tests/unit/poll-card.test.mjs`

### Change Log

- 2026-08-02: Story 1.11 creator dashboard — poll-card component/tokens, listPollsForOwner, `/creator` rewrite, two-column detail with monitor floor, unit/integration/e2e proofs, CHANGELOG.
- 2026-08-02: Code review — resolved 2 architecture decisions and applied all 10 patches; full release-equivalent gate green; story completed.
