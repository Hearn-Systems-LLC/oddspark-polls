---
baseline_commit: f46064d8cda9a71d9bfc6af4d66703818a5b7e73
---

# Story 1.8: Results View with Visibility Settings

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->
<!-- Branch provenance: `story/1-8-results-view-with-visibility-settings` was cut from fetched `origin/main` at `f46064d8cda9a71d9bfc6af4d66703818a5b7e73`, the merge of Story 1.7. -->

## Story

As a Voter or Creator,
I want to see the Tally rendered per the Poll's Visibility Setting,
so that results are visible to exactly the audience the Creator chose.

## Acceptance Criteria

1. **Given** an open Poll with Visibility **Live**, **When** anyone with the link opens `/{link}/results`, **Then** the Tally renders as `results-bar` blocks — 34/38px bars on 1px baseline rules, wash fill with a 2px leading edge, label left and `{pct}% · {count}` inside, the leader carrying gold wash/edge plus the `◆` marker, an exact tie withdrawing all gold and `◆` under a `TIED` line (UX-DR3), **And** all counts and percentages are computed server-side from accepted Vote facts via SQL projection (AD-9, NFR-6).
2. **Given** a Voter who has just voted, **When** the confirmation and Tally render, **Then** their own choice appears as a text-only `YOUR BALLOT` line — never a second gold on the surface.
3. **Given** Visibility **After Close** on an open Poll, **When** a Voter who voted (or anyone hitting `/{link}/results` directly) looks, **Then** they see the confirmation/explanation shape — `Results open when the Poll closes — {deadline, local}.` — with the question, no counts, no totals, nothing leaking the result's shape (FR-20, UX-DR19), **And** a Poll with no Deadline (manual-close-only) renders the no-timestamp variant — `Results open when the Poll closes.` — same shape, no hole in the sentence.
4. **Given** Visibility **Creator-Only**, **When** anyone but the authenticated owning Creator requests results, **Then** they see `These results go to the Creator only.` with no counts, while the owner sees the full Tally, **And** every result query authorizes a `ViewerContext` before reading private facts, and creator-only or not-yet-visible responses are served `private, no-store` (AD-21/AR-17).
5. **Given** a Poll with no Votes, **When** the Tally renders, **Then** zero-width bars sit on their baseline rules with labels and the empty-state line (`No Votes yet. Yours would be the first, which is a kind of power.`), and a cold load shows skeleton bars at the correct count with no shimmer, resolving without animation (UX-DR19).

## Tasks / Subtasks

- [x] Task 1: Establish the Results application boundary and visibility policy before touching delivery code (AC: #1, #3, #4, #5)
  - [x] UPDATE `src/modules/results/index.ts`, replacing the placeholder with a framework-free Results application query. Define a Results-owned `ViewerContext` containing only the authenticated internal `userId` or anonymous state, a small access envelope port, a tally-projection port, and a closed outward view union such as `visible | after_close_hidden | creator_only_hidden | not_found`; do not import Astro, D1, Cloudflare, session cookies, or route types into the module (AD-1, AD-19)
  - [x] The access envelope contains only facts needed to decide entitlement or render the permitted hidden shape: internal Poll ID, question, `resultVisibility`, `ownerUserId`, `deadlineMs`, `closedAtMs`, `multiSelectEnabled`, and canonical reference. `multiSelectEnabled` is safe Poll configuration, not a result fact. The envelope MUST NOT contain options, option count, Vote counts, percentages, `representationVersion`, validators, or any other result-shape/private signal; the authorized tally projection supplies option labels/counts for visible and loading renderers
  - [x] Compute effective closure on every query using the existing shared `effectivePollStatus` rule (`closedAtMs !== null || deadlineMs <= now`); After Close opens without a scheduler or persistence write. Live is visible open or closed. After Close is hidden while open and fully visible once effectively closed. Creator-Only is visible only when `viewer.userId === ownerUserId`; an anonymous viewer, signed-in non-owner, expired session, or failed session lookup is not entitled
  - [x] Enforce an observable two-step call order: resolve access envelope → authorize → and only for `visible` call the private tally projection port. Unit fakes must prove the tally port is never invoked for either hidden union member or `not_found`; do not fetch then redact (AD-21/AR-17)
  - [x] Keep `result_visibility` independent from discovery/listing state. A listed, unlisted, or delisted Poll reached by a valid link receives the same Results decision; do not read `discovery_state` to authorize the Tally (AD-5)
  - [x] Define the Results outward projection in the Results module rather than widening the public `PollPage` DTO. Preserve Creator-authored option order. Carry `multiSelectEnabled` explicitly plus per-option internal ID, label, position, accepted selection count, computed whole-number percentage, aggregate Voter count, and total selection count; never infer Poll mode from current counts and never carry a claim digest, submission ID, browser token, or voter-linked ballot fact. Render the multi-select summary even when there are zero Votes or every Voter selected exactly one option
  - [x] `[ASSUMPTION: percentage denominator]` For single- and multi-select Polls, calculate each option as `Math.round(optionCount / voterCount * 100)` — the percentage of distinct Voters who selected it. Multi-select percentages may intentionally total above 100, while the `N VOTERS · M SELECTIONS` line explains the shape. A zero Voter denominator yields exactly 0, never `NaN`/`Infinity`; this resolves the deferred Story 1.1 input-safety item in `deferred-work.md`
  - [x] Derive leader/tie presentation from positive counts only: one unique positive maximum is the sole leader; two or more options sharing a positive maximum produce `TIED` and no leader; all-zero is the empty state, not a tie. Do not sort by count or move option rows
  - [x] Unit-first (`tests/unit/results.test.ts`): table-test the full Live/After Close/Creator-Only matrix, deadline boundary (`now === deadlineMs` is closed), no-deadline manual-open behavior, owner vs non-owner, absent Poll, hidden-port non-invocation, zero denominator, nearest-integer rounding (`57/122 → 47`), multi-select `voterCount < selectionCount`, unique leader, exact positive tie, and all-zero non-tie

- [x] Task 2: Add one D1-backed accepted-fact Tally projection, with authorization structurally ahead of it (AC: #1, #4, #5)
  - [x] UPDATE `src/adapters/d1/index.ts` with a Results-specific access read and a separate private tally read implementing Task 1's ports. Reuse existing row-to-domain validation idioms and shared ID/enum owners; do not broaden `PollPage` with owner/private projection fields just to serve this route
  - [x] The access read may join `poll_reference` to the minimum Poll metadata required by the safe hidden shape, but it must not read options or join/aggregate `vote`/`vote_selection`. Its inbound caller sets `requestContext.pollId` only after a real Poll is resolved; the D1 adapter remains unaware of request context. A missing/deleted reference remains the same plain 404 and emits no user-supplied reference as a telemetry identifier
  - [x] Implement the visible tally as a single prepared SQL projection over committed `vote` and `vote_selection` rows. It must: preserve every `poll_option` with a left join even at zero count; scope selections through `vote.poll_id` as well as option Poll ownership so independently valid cross-Poll foreign keys cannot contaminate a Tally; compute distinct Vote/Voter count and total selection count from the same statement/snapshot; and order by `poll_option.position`
  - [x] Use D1 prepared statements with positional `.bind(...)` values and typed `.all<T>()` results; never interpolate a reference or ID into SQL. Map integer counts defensively to finite non-negative domain values and fail closed on malformed rows rather than emitting misleading percentages
  - [x] Reuse Story 1.7's pure `projectResults` as contract precedent/unit oracle only. Do NOT load all Vote rows into application memory to call it in production: AD-9 and NFR-6 require the runtime projection in SQL. Do not widen `MultipleChoicePersistedFacts` or the shared Poll-type kernel
  - [x] No migration is expected: migrations 0004/0006/0008 already provide Poll visibility/ownership/status/options and accepted Vote/selection facts. Do not edit an existing migration or its checksum; if implementation discovers a genuine schema gap, stop and correct this story/architecture before inventing a migration
  - [x] Integration-first (`tests/integration/results-adapter.integration.test.ts`): real workerd + D1 fixtures for no Votes/all zero options, single-select counts, multi-select Voters vs selections, multi-select with zero Votes, multi-select where each Voter selected exactly one option, stable position ordering, an option with zero selections, exact tie, a selection row whose Vote belongs to another Poll not leaking into this Poll, missing Poll, and the safe access-read/private-projection separation

- [x] Task 3: Turn the results-bar skeleton into the shared static Tally component (AC: #1, #2, #5)
  - [x] UPDATE `src/components/results-bar.astro` without changing the design tokens in `src/styles/tokens.css`: 34px mobile/38px desktop, 4px group gap supplied by the parent, 1px baseline, entropy wash + 2px entropy edge, label left, and the value cluster right. Percentage uses data typography; the raw count uses caption typography. Initial/final width has no CSS transition in this story
  - [x] Move the decorative `◆` immediately before the value cluster, not before the label. The sole leader uses solar wash/edge. Set the bar accessible name to the established form `Pizza, 47 percent, 122 votes, leading` (omit `, leading` otherwise); hide the diamond from assistive technology. Clamp/reject non-finite input and suppress the 2px edge at zero width while retaining the baseline and label
  - [x] NEW `src/components/results-tally.astro` as the one renderer reused by direct results and the post-vote surface. It owns stable option order, the `N VOTERS · M SELECTIONS` label-caps-lg summary for multi-select, optional `TIED`, all-zero empty copy, the bar group, and optional text-only `YOUR BALLOT` label/value. `YOUR BALLOT` joins multiple selected labels in stored option order with ` · `, carries no gold/diamond, and never echoes selected rows
  - [x] Render ties and leadership without color alone: `TIED` is visible text; the unique leader carries both gold and the decorative diamond plus its accessible-name suffix. On the empty state show all labels and zero-width baselines, the exact empty copy, no gold, no diamond, and no `TIED`
  - [x] Give the shared component an explicit, exercised `loading` variant with exactly the real option count/labels, zero-width labelled bars, baseline rules, no shimmer, and no animation. Render the authorized final Tally in the server HTML as the no-JS floor, and add a minimal fail-open progressive cold-load state that presents the skeleton for one painted frame, then reveals the already-rendered final Tally on the next frame with no transition. Do not fetch result data client-side, embed hidden result JSON, use `server:defer`, or leave final content unavailable when JavaScript is disabled or the enhancer fails. A component-only unused variant does not satisfy AC #5
  - [x] The loading group exposes `aria-busy="true"` and loading-specific option names; it must not announce skeleton values as real `0 percent, 0 votes` results. On resolution, remove the busy state and expose the final exact bar names. Keep the skeleton presentation non-interactive and excluded from duplicate reading when final content becomes active
  - [x] Preserve the homepage demonstration consumers of `results-bar.astro` while correcting their markup; update `src/pages/index.astro` only if its now-stale “voting/results later” copy or fixture props require it. The smoke-test `--color-solar-dark` marker remains intact
  - [x] Component/unit coverage asserts zero/finite width normalization, value typography separation, accessible names, sole leader, all tied bars blue, no two-golds condition, correct skeleton row count/labels/no shimmer, and no mount/arrival transition

- [x] Task 4: Add the canonical direct Results route as a thin inbound adapter (AC: #1, #3, #4, #5)
  - [x] NEW `src/pages/[reference]/results.astro` for safe GET/HEAD navigation. The Results application access read is the exact reference lookup — do not perform a preliminary `PollPage` existence read and then query the same reference again. Only a `not_found` Results outcome may trigger the existing bounded case-folded custom-link fallback; a hit 301s to `/{canonical}/results` preserving the query string, while a mixed-case generated-reference miss stays 404. Do not duplicate or relax the reserved-name registry
  - [x] Build `ViewerContext` only from `Astro.locals.principal?.userId` (the internal Oddspark user ID), call the Results application query, and render its closed union. The D1 port returns internal Poll ID but never sees `RequestContext`; after any non-`not_found` Results outcome, this inbound adapter sets `Astro.locals.requestContext.pollId`, while a missing reference leaves it unset. Delivery composes headers/status/templates but contains no visibility, closure, leader, percentage, or ownership business rule
  - [x] For Live, closed After Close, and owning Creator-Only, render the question plus shared full Tally. Direct results is centered/single-column at all widths; only the post-vote surface gets the large two-column composition
  - [x] For open After Close, render the question and exact explanation only: with deadline `Results open when the Poll closes — {deadline, local}.`; without deadline `Results open when the Poll closes.` Reuse/extract the existing local-deadline progressive enhancement so server markup remains meaningful and the timestamp becomes viewer-local when JavaScript runs. Emit no counts, totals, percentages, widths, leader/tie state, option-result rows, result JSON/data attributes, ETag, representation version, or result-bearing accessible label
  - [x] For hidden Creator-Only, render the question and exactly `These results go to the Creator only.` A signed-in non-owner is identical to anonymous. Do not add a sign-in prompt that reveals owner entitlement and do not distinguish session lookup failure from anonymity
  - [x] Serve every Results page `Cache-Control: private, no-store` (a safe rule stricter than the minimum hidden-state requirement), preserve `x-request-id` through middleware, and never use the Cache API/shared caches. Return a plain indistinguishable 404 for nonexistent/deleted Polls. Reject unsupported mutation methods before any private tally read
  - [x] Keep the result document lightweight and SSR-first: no framework hydration, no conditional polling, no live endpoint, no ETag/version validator, no chart toggle, and no animation. Those belong to Stories 1.9 and 1.10. Do not fabricate a trust badge/security-toggle state before Epic 2, and do not pull in comments, export, ranked/image/meeting result renderers, or Ballot Manifest work

- [x] Task 5: Reuse the same authorized Tally on vote outcomes without regressing the hardened voting flow (AC: #2, #3, #4, #5)
  - [x] UPDATE `src/pages/[reference].astro` only after Story 1.7 is on main. After a counted/replayed/already-voted/closed read-only outcome, call the same Results application query with the current principal and render the same union/component; do not duplicate visibility policy or query counts inside the route
  - [x] Preserve the full read-only option list on closed and already-voted surfaces where the experience contract requires the Poll/options to remain on screen, but suppress every selected/gold marker there. Counted confirmation may use the compact confirmation/ballot/Tally composition; the shared Tally integration is not authorization to remove required read-only options from every outcome
  - [x] Only after the Results outcome is `visible`, resolve the viewer's own labels through the existing claim lookup, order them by Poll option position, and render the shared text-only `YOUR BALLOT` block when a claim resolves. Do not call `findVoteSelectionByClaim` for hidden After Close or unauthorized Creator-Only outcomes. Only the unique leading bar may be gold; an own ballot never adds a second gold/diamond
  - [x] When open After Close is hidden, keep the successful Counted confirmation and show the exact deadline/no-deadline explanation with question but no result-shape facts. When Creator-Only is hidden, keep the successful Counted confirmation and exact creator-only explanation. An owning Creator visiting the same surface may receive the full Tally under the same policy
  - [x] Preserve all Story 1.5–1.7 invariants byte-for-byte unless the shared results rendering requires a localized edit: replay-before-limiter, idempotency conflict, fresh nonce on 422, ballot preservation, claim semantics, rate-limit behavior, deadline closure, case/canonical chain, `private, no-store`, first-in-main focused outcome, title prefix, offline/page-show recovery, and multi-select client bounds
  - [x] At `{breakpoints.lg}` and up, compose post-vote confirmation/`YOUR BALLOT` left and Tally right; below it, keep one reading-order column with confirmation/ballot before Tally. Never use positional copy such as “results below,” and hide nothing on mobile
  - [x] UPDATE existing `tests/e2e/vote.spec.mjs` assertions that currently expect cast-option gold/read-only rows: distinguish counted, already-voted, and closed states; assert their required full-option vs compact composition, exact `YOUR BALLOT` text for entitled single and multi-select ballots, option order, no selected marker/no duplicate gold/diamond, focused Counted outcome, and visibility-specific no-leak behavior

- [x] Task 6: Prove privacy, presentation, SSR resilience, and scope boundaries end to end (AC: all)
  - [x] NEW `tests/e2e/results.spec.mjs` following the existing Playwright/dev-server fixture style. Cover Live open/closed for anonymous/owner/non-owner; After Close open with deadline and manual-close variants; After Close becoming visible from deadline comparison without a write; Creator-Only owner vs anonymous/signed-in non-owner; zero votes; exact tie; unique leader; multi-select totals/percentages; missing/deleted references; canonical case redirect preserving `/results` and query; and GET/HEAD/mutation safety
  - [x] No-leak assertions inspect raw HTML, rendered accessibility tree/labels, response headers, and any inline data for forbidden counts, totals, percentages, widths, option result rows, ETag, or representation version in open After Close and unauthorized Creator-Only responses. Unit tests separately prove the tally port and claim-selection lookup were not called before entitlement
  - [x] Accessibility assertions: visible `TIED`; decorative diamond excluded from the accessible name; leader named semantically; empty/skeleton baselines remain legible; question precedes explanation/Tally in reading order; no new interactive controls; no result state conveyed only by color
  - [x] Browser proof is mandatory for this browser-observable story: capture the affected post-vote and direct-results surfaces at 375px dark and at least 1024px light, inspect a positive tie and unique leader/`YOUR BALLOT`, and record a clean browser console. Do not ask Justin to perform the visual check
  - [x] Verify the JavaScript-disabled route contains and displays the final authorized Tally, and that hidden states remain fully useful without JavaScript. On the actual authorized route, observe the cold skeleton's correct row count/labels and busy semantics, then its resolution to final bars with no width animation; an isolated component fixture alone is insufficient
  - [x] Add adversarial Creator-authored question and option labels containing HTML/script-like text. Assert Astro escapes them on visible and hidden Results surfaces, no element/attribute injection occurs, and the browser console records no execution/error (NFR-8)
  - [x] Assert the inbound route sets internal `requestContext.pollId` for visible and hidden resolved Polls and leaves it unset for `not_found`, without putting the public reference or result facts into telemetry
  - [x] UPDATE `CHANGELOG.md` under `## [Unreleased]` with the user-visible Results route/visibility behavior. Update `README.md` only if setup, bindings, environment topology, or public route documentation actually changes; no architecture/UX doc edit is expected because those sources already specify this topology
  - [x] Run narrow red/green tests during each task, then the repository gate in documented order: `pnpm migrations:guard`, `pnpm test`, `pnpm check`, `pnpm test:e2e`, `pnpm types`, `git diff --exit-code worker-configuration.d.ts`, `pnpm build:production`. No binding change is expected, but the generated-type drift assertion remains part of the gate

### Review Findings

- [x] [Review][Patch] Resolve the requested reference separately from its canonical reference; the canonical-only join makes an otherwise resolvable link return `not_found` [src/adapters/d1/index.ts:427]
- [x] [Review][Patch] Scope Tally aggregation through the target Poll's indexed Vote rows; the current join builds an automatic index over the global `vote_selection` table [src/adapters/d1/index.ts:473]
- [x] [Review][Patch] Fail closed when a resolved Poll has Votes but no option rows; the empty result currently overwrites aggregate totals with zero [src/adapters/d1/index.ts:498]
- [x] [Review][Patch] Reject or faithfully represent a nonzero-Voter projection with zero valid selections; the current `empty` flag renders `No Votes yet` beside a nonzero total [src/modules/results/index.ts:155]
- [x] [Review][Patch] Require a positive maximum before marking a sole option as leader; one zero-count option is currently both empty and leading [src/modules/results/index.ts:150]
- [x] [Review][Patch] Preserve the committed vote/read-only outcome when the Results query fails, render an explicit Results-unavailable state, and flag the existing single telemetry record as an error instead of returning a silent successful omission [src/pages/[reference].astro:531] — severity: medium
- [x] [Review][Patch] Render the complete authorized Results union on read-only vote surfaces and derive Counted copy from that union; hidden branches are discarded today, so already-voted viewers miss the required explanation and a deadline/visibility race can show a visible Tally beside stale hidden copy [src/pages/[reference].astro:529] — severity: high
- [x] [Review][Patch] Make the cold-load state coherent and actually paint it; final totals, `TIED`, and empty copy remain visible beside loading bars, while the single `requestAnimationFrame` can replace the skeleton before any frame displays it [src/components/results-tally.astro:45] — severity: medium
- [x] [Review][Patch] Constrain valid unbroken Creator text on the new Results surfaces; option labels can grow past the fixed 34/38px bar and displace its value, while long questions and `YOUR BALLOT` values can create horizontal overflow [src/components/results-bar.astro:99] — severity: medium
- [x] [Review][Patch] Refresh an open After-Close Results page when it returns from the back-forward cache after its deadline; otherwise the restored page can keep withholding a Tally that is now visible [src/scripts/results-page.ts:8] — severity: medium
- [x] [Review][Patch] Use the normalized percentage for visible and accessible values as well as width; current defensive clamping can render a safe width labelled `NaN%`, `Infinity%`, or `150%` [src/components/results-bar.astro:33] — severity: low
- [x] [Review][Patch] Restore the required 4px parent gap in the homepage ResultsBar demonstration instead of overriding it to zero [src/pages/index.astro:92] — severity: low
- [x] [Review][Patch] Correct Story 1.8 baseline provenance to the actual branch-cut `origin/main` commit `f46064d8cda9a71d9bfc6af4d66703818a5b7e73` and remove the stale pre-merge prerequisite/intelligence; the recorded `9ecaf787cc52689ef383a9ee21597f3811bbdad2` baseline expands review scope with unrelated Story 1.7 work [_bmad-output/implementation-artifacts/1-8-results-view-with-visibility-settings.md:2]
- [x] [Review][Patch] Normalize telemetry operations for dynamic Poll routes so public or user-supplied references never enter Workers Logs, and assert the fixture references are absent while the internal `pollId` remains present [src/middleware.ts:256]
- [x] [Review][Patch] Make the documented `pnpm test:e2e` gate deterministic against its shared local D1 store, then rerun that exact command; the checked gate currently records only a one-worker full-suite pass while the configured ten-worker command reproduces SQLite/Miniflare failures [playwright.config.ts:7]
- [x] [Review][Patch] Harden the auxiliary telemetry server by avoiding a fixed-port/stale-process readiness false positive, detecting early child exit, preventing shared-D1 process contention, and awaiting process-group shutdown with a bounded kill fallback [tests/e2e/results.spec.mjs:1003]
- [x] [Review][Patch] Give Results fixtures run-scoped references and make teardown attempt every seeded owner; fixed global references plus `afterAll`-only cleanup make an interrupted or partially failed run poison later retries with unique-reference collisions [tests/e2e/results.spec.mjs:62]
- [x] [Review][Patch] Strengthen hidden-results privacy checks for the signed-in non-owner and post-vote branches so stray count, percentage, total, leader/tie, or result-bearing attributes fail the suite rather than only known Tally selectors; keep the public option labels intentionally required on full read-only vote surfaces [tests/e2e/results.spec.mjs:397]
- [x] [Review][Patch] Extend captured-log coverage to prove each request emits exactly one record and a handled `200` Results-projection failure records `result: "error"`; the current `Map` hides duplicate emissions and the failure flag is tested only in the pure mapper [tests/e2e/results.spec.mjs:1063]
- [x] [Review][Patch] Add the claimed structural regression proof that `findVoteSelectionByClaim` is never called before a `visible` Results decision; current tests cover the tally port but never observe the claim-selection port [src/pages/[reference].astro:598]
- [x] [Review][Patch] Complete the claimed Live end-to-end matrix by asserting the anonymous open Tally and owner/non-owner closed Tally, not only headers for anonymous-open and content for anonymous-closed [tests/e2e/results.spec.mjs:173]
- [x] [Review][Patch] Move the pre-vote After-Close deadline fixture well beyond one minute so slow local-D1 or CI execution cannot close the Poll before the vote under test is accepted [tests/e2e/vote.spec.mjs:1598]
- [x] [Review][Patch] Capture the mandatory direct-results proof at desktop light width as well as mobile dark; the current desktop-light screenshot covers only the post-vote surface [tests/e2e/results.spec.mjs:890]
- [x] [Review][Patch] Reconcile Task 3's checked “first browser frame” promise with the ratified and tested one-painted-frame skeleton followed by reveal on the next frame [_bmad-output/implementation-artifacts/1-8-results-view-with-visibility-settings.md:53]
- [x] [Review][Patch] Describe the post-vote Tally as beside or below the confirmation according to viewport instead of always “beneath” it [CHANGELOG.md:64]
- [x] [Review][Defer] Editing an existing option through a temporarily blank value can clamp multi-select bounds and never restore them when the label is replaced — deferred, pre-existing [src/scripts/create-poll-form.ts:103]

## Dev Notes

### Product and UX Contract

- This story closes the core-loop visibility seam: FR-20 governs exactly three settings (`live`, `after_close`, `creator_only`); FR-4 means a closed Poll renders the Tally whenever that setting permits. Direct requirements trace to UJ-1/UJ-2/UJ-6/UJ-7, NFR-3 owner authorization, NFR-4 privacy, NFR-5 lightweight SSR, NFR-6 server SQL, NFR-8 escaped Creator text, and NFR-10 craft/accessibility [Source: `_bmad-output/planning-artifacts/prds/prd-oddspark-polls-2026-07-28/prd.md`; `_bmad-output/planning-artifacts/epics.md:426-455`]
- Exact Results behavior is already resolved by the experience source: direct hidden states show the Poll question and explanation rather than 404/redirect; a missing/deleted Poll alone is a plain 404. Loading is labelled zero-width baselines without shimmer; empty retains the same shape; own ballot is text-only [Source: `_bmad-output/planning-artifacts/ux-designs/ux-oddspark-polls-2026-07-28/EXPERIENCE.md:178-196`]
- Results bars consume the existing `results-bar` component tokens. Do not restate 34/38px, 2px edge, colors, or typography as component-local raw values. Gold is exclusive to one leader on the Tally; exact positive ties withdraw it [Source: `_bmad-output/planning-artifacts/ux-designs/ux-oddspark-polls-2026-07-28/DESIGN.md:168-190,440-459,489-493`]
- `[ASSUMPTION: percentage denominator]` Use “share of Voters who selected this option,” rounded to the nearest whole number. The mock ratifies nearest-integer rounding (`57/122 → 47`) and the multi-select contract ratifies separate Voter/selection totals, but neither source explicitly names the multi-select denominator; record the final denominator decision in completion notes. Under this assumption multi-select percentages may intentionally total above 100% [Source: `_bmad-output/planning-artifacts/ux-designs/ux-oddspark-polls-2026-07-28/mockups/key-tally.html:220`; `_bmad-output/planning-artifacts/epics.md:422-424`]
- Responsive behavior is one column below `lg`; only the post-vote Tally becomes two columns at `lg`, with ballot/confirmation left and bars right. The direct Results page remains centered. Everything survives no-JS and no copy refers to position [Source: `_bmad-output/planning-artifacts/ux-designs/ux-oddspark-polls-2026-07-28/EXPERIENCE.md:228-242`]
- Accessible bar naming is `Pizza, 47 percent, 122 votes, leading`; state cannot be color-only. Story 1.9 will own aggregate polite live announcements, so 1.8 should render semantically structured static totals but add no polling/live chatter [Source: `_bmad-output/planning-artifacts/ux-designs/ux-oddspark-polls-2026-07-28/EXPERIENCE.md:244-259`]

### Architecture Guardrails

- The route is an inbound adapter, `src/modules/results` owns application policy/outward projection, D1 implements outbound ports, and shared domain remains the sole owner of `ResultVisibility`/effective-state semantics. No business rule belongs in Astro frontmatter (AD-1, AD-11, AD-19, AD-23) [Source: `_bmad-output/planning-artifacts/architecture/architecture-oddspark-polls-2026-07-29/ARCHITECTURE-SPINE.md:30-60,194-201,290-346,477-480`]
- D1 Vote facts are authoritative and the Tally is disposable. Runtime multiple-choice counts come from raw accepted facts in SQL, not a stored counter and not a JavaScript fold over all Votes (AD-6, AD-9) [Source: `_bmad-output/planning-artifacts/architecture/architecture-oddspark-polls-2026-07-29/ARCHITECTURE-SPINE.md:109-115,169-175`]
- Authorization before private-fact retrieval is load-bearing. The Results query accepts `ViewerContext`; hidden responses reveal no aggregate, timing/version, manifest, or cache validator. Result/manifests never enter a shared cache; use `private, no-store` for the whole route as the simplest correct rule (AD-21/AR-17) [Source: `_bmad-output/planning-artifacts/architecture/architecture-oddspark-polls-2026-07-29/ARCHITECTURE-SPINE.md:318-327`]
- Use the internal principal user ID supplied by middleware, never Google/GitHub identifiers. Preserve one voter-blind telemetry record and set only internal Poll ID/operation/result metadata; no selection, count tied to a voter, digest, or ballot content enters logs (AD-4, AD-13) [Source: `_bmad-output/planning-artifacts/architecture/architecture-oddspark-polls-2026-07-29/ARCHITECTURE-SPINE.md:80-89,244-252`; `src/middleware.ts`]
- Do not pull forward conditional polling, `representation_version`/ETag validators, reconnect state, live dot, count-up/width animation, leader cross-fade, or chart toggle. These are explicitly Stories 1.9 and 1.10 [Source: `_bmad-output/planning-artifacts/epics.md:457-507`]

### Previous Story Intelligence

- Story 1.7 already ships `projectResults` with per-option accepted-selection counts, distinct Voter count, and total selection count, plus multi-select option ordering and own-selection claim recovery. Its story deliberately deferred SQL and rendering to 1.8; honor that boundary rather than redesigning 1.7 [Source: `_bmad-output/implementation-artifacts/1-7-multi-select-voting.md`]
- Current `src/modules/polls/types/multiple-choice.ts`'s pure projector loops over in-memory fixtures and its output lacks labels/percentages; it is a useful contract test, not the runtime AD-9 path. Current `src/modules/results/index.ts` is the intended placeholder seam
- Current `src/adapters/d1/index.ts` has public Poll/options and Vote/claim reads but no Results projection. `PollPage` has visibility/deadline/status fields, not owner; introduce a purpose-shaped Results access envelope instead of leaking ownership into every public Poll load
- Current vote page already has the hardened exact/case-folded canonical chain, local-deadline enhancement, visibility-specific Counted copy, effective closure, and claim-to-own-option resolution. Reuse or extract those seams. Its read-only own-choice rows currently use gold markers and therefore must be replaced when a Tally renders
- Story 1.8 was cut from fetched `origin/main` at `f46064d8cda9a71d9bfc6af4d66703818a5b7e73`, the merge commit containing the complete Story 1.7 branch; the exact branch-cut provenance is recorded in frontmatter

### Current Technical Information

- Keep the repository-pinned stack: Astro 7.1.5 with `@astrojs/cloudflare` 14.1.6, TypeScript 7.0.2, Wrangler 4.115, Better Auth 1.6.25, Zod 4.4.3, Vitest 4.1.10, and Playwright 1.62. Story 1.8 requires no dependency or runtime upgrade [Source: `package.json`; `pnpm-lock.yaml`; `AGENTS.md`]
- Astro dynamic route params come from `Astro.params`; SSR page code can set response status/headers through the Astro response API. The nested file shape `src/pages/[reference]/results.astro` is the intended static path segment under a dynamic Poll reference [Source: Astro API Reference, https://docs.astro.build/en/reference/api-reference/]
- Cloudflare D1 prepared statements accept bound parameters and typed result generics; use `.bind(...).all<T>()` for aggregate rows. D1 SQL follows SQLite semantics, so write an explicit grouped projection and test it in workerd rather than relying on database-specific behavior [Source: Cloudflare D1 Worker API, https://developers.cloudflare.com/d1/worker-api/; Prepared statements, https://developers.cloudflare.com/d1/worker-api/prepared-statements/; SQL API, https://developers.cloudflare.com/d1/sql-api/sql-statements/]
- Cloudflare may cache GET responses unless explicitly controlled; `Cache-Control: private, no-store` is the positive exclusion contract for these result-bearing/withheld responses [Source: Cloudflare Workers Cache configuration, https://developers.cloudflare.com/workers/cache/configuration/]
- Context7 was configured but unavailable during story creation on 2026-07-31, so current syntax was cross-checked against the official Astro and Cloudflare documentation above. At implementation start, retry Context7 for the pinned Astro 7 and Wrangler/D1 APIs before changing library-specific code, per repository instructions

### Project Structure Notes

- NEW `src/pages/[reference]/results.astro` — direct Results inbound adapter
- NEW `src/components/results-tally.astro` — shared direct/post-vote static Tally and explicit loading state
- NEW `tests/unit/results.test.ts`, `tests/integration/results-adapter.integration.test.ts`, `tests/e2e/results.spec.mjs`
- UPDATE `src/modules/results/index.ts`, `src/adapters/d1/index.ts`, `src/components/results-bar.astro`, `src/pages/[reference].astro`, `tests/e2e/vote.spec.mjs`, and `CHANGELOG.md`
- CONDITIONAL UPDATE `src/pages/index.astro`, existing unit/integration test files, and the isolated deadline script only where reuse/fixture compatibility requires it
- PRESERVE `src/styles/tokens.css` (tokens already match the design), `src/shared/*` (enum/effective-state owner), `db/migrations/*`, `wrangler.jsonc`, `worker-configuration.d.ts`, and package manifests unless a proven contradiction is escalated before implementation
- Do not create a parallel design/decision record, `build_log.md`, or `docs/decisions/`; implementation decisions and evidence belong in this story's Dev Agent Record per repository policy

### References

- [Source: `AGENTS.md` — stack, verification, testing, git flow, source-of-truth, privacy, middleware, and migration rules]
- [Source: `_bmad-output/planning-artifacts/epics.md:400-507` — Story 1.7 handoff, Story 1.8 exact contract, Stories 1.9/1.10 exclusions]
- [Source: `_bmad-output/planning-artifacts/prds/prd-oddspark-polls-2026-07-28/prd.md` — FR-4, FR-20/21, FR-28; UJ-1/2/6/7; NFR-3/4/5/6/8/10/11]
- [Source: `_bmad-output/planning-artifacts/prds/prd-oddspark-polls-2026-07-28/addendum.md` — ratified product clarifications]
- [Source: `_bmad-output/planning-artifacts/architecture/architecture-oddspark-polls-2026-07-29/ARCHITECTURE-SPINE.md:30-60,80-115,169-201,244-252,290-346,477-480`]
- [Source: `_bmad-output/planning-artifacts/ux-designs/ux-oddspark-polls-2026-07-28/DESIGN.md:168-190,440-493,548-569`]
- [Source: `_bmad-output/planning-artifacts/ux-designs/ux-oddspark-polls-2026-07-28/EXPERIENCE.md:81-86,178-200,220-259`]
- [Source: `_bmad-output/planning-artifacts/ux-designs/ux-oddspark-polls-2026-07-28/mockups/key-tally.html`]
- [Source: `_bmad-output/implementation-artifacts/1-7-multi-select-voting.md`; `deferred-work.md`]
- [Source: `src/modules/polls/types/multiple-choice.ts`; `src/modules/results/index.ts`; `src/adapters/d1/index.ts`; `src/pages/[reference].astro`; `src/components/results-bar.astro`; `src/middleware.ts`]
- [Source: Astro API Reference, https://docs.astro.build/en/reference/api-reference/]
- [Source: Cloudflare D1 Worker API, https://developers.cloudflare.com/d1/worker-api/; Prepared statements, https://developers.cloudflare.com/d1/worker-api/prepared-statements/]
- [Source: Cloudflare Workers Cache configuration, https://developers.cloudflare.com/workers/cache/configuration/]

## Dev Agent Record

### Agent Model Used

OpenAI Codex (GPT-5)

### Debug Log References

- Task 1 red→green: `tests/unit/results.test.ts` failed 20/20 against the placeholder module, then passed 20/20 after implementing `queryResults` (`pnpm vitest run --project unit tests/unit/results.test.ts`).
- Task 2 red→green: `tests/integration/results-adapter.integration.test.ts` failed 10/10 before `createResultsPersistence` existed, then passed 10/10 in workerd against real D1 (`pnpm vitest run --project integration tests/integration/results-adapter.integration.test.ts`).
- Task 3: helper unit tests (`tests/unit/results-bar.test.ts`, 11/11); DOM-level component behaviors (skeleton rows/labels/busy, no shimmer/transition, tied-blue, leader name, typography separation) are asserted in the e2e suite on the real route, since an isolated component fixture is explicitly insufficient for AC #5.
- Tasks 4–5: smoke-verified against a local dev server (visible/hidden/404/301/405 shapes, Counted→YOUR BALLOT→Tally flow), then `tests/e2e/vote.spec.mjs` iterated to green: YOUR BALLOT label/value spans assert separately (no whitespace between spans in textContent), option-label assertions rescoped to `.poll-option-readonly` (strict-mode collisions with bar labels), and no-leak assertions rescoped to `<main>` because Astro hoists statically-imported component CSS into `<head>` even when the component never renders.
- Task 6: `tests/e2e/results.spec.mjs` iterated to 15/15 (dev-toolbar buttons excluded from interactive-control counts; adversarial-label assertions check for injected elements/attributes rather than escaped text content). Browser proof screenshots recorded under `test-results/results-proof/` (375px dark leader + tie, 1280px light post-vote two-column, 375px dark post-vote single column; clean console enforced per test).
- Full-suite e2e showed two single-test, nondeterministic failures in vote.spec across three full runs (a 500 under parallel D1 load and a transient session-lookup degradation → sign-in redirect); a third full run passed 74/74. Both are local-miniflare/CLI SQLite lock contention in fixture paths, not product regressions; CI's configured retry (`retries: 1`) covers this class.

### Completion Notes List

- Chunk 3 code review (2026-08-02): resolved all thirteen patch findings. The story now records the actual `origin/main` branch-cut SHA; dynamic Poll telemetry uses reference-free route templates on success and failure; claim-selection reads pass through a framework-free visible-results gate; and the browser harness runs one shared-D1 worker with run-scoped references, complete cleanup attempts, robust auxiliary-server lifecycle handling, exact request-ID telemetry cardinality/error/privacy assertions, stronger hidden-result checks, the complete Live audience/status matrix, a durable deadline fixture, and desktop-light direct-results proof. The responsive changelog and painted-frame task wording now match the implementation. The pre-existing transient option-edit bounds clamp is recorded in `deferred-work.md`. Under Node 24.18.0, the exact repository gate passed: migration guard (8 checksummed migrations), 455 Vitest tests, TypeScript check, 78/78 Playwright tests via `pnpm test:e2e`, generated binding-type drift check, and production build. The new 1280px light direct-results and post-vote proof screenshots were inspected with a clean console enforced by the suite.
- Chunk 1 code review (2026-08-02): resolved all five findings. Exact aliases now resolve through their Poll while returning the canonical reference; the accepted-fact projection starts from target-Poll Votes and existing indexes; malformed no-option or fewer-selection-than-Voter states fail closed; aggregate invariants are validated; and an all-zero sole option can never lead. Added focused unit/integration regressions. Repository gate passed under Node 24.18.0: migration guard, 436 Vitest tests, type check, 74 Playwright tests, binding-type drift check, and production build. The first parallel Playwright run hit two documented local Miniflare/D1 contention failures; both passed in isolation and the full rerun passed 74/74.
- Chunk 2 code review (2026-08-02): resolved all seven findings. Read-only vote surfaces now render every authorized Results outcome, keep Counted/read-only state when the additive Results query fails, show explicit unavailable copy, and fold that failure into the existing telemetry record. Cold-load metadata and bars switch together after a painted frame; long Creator text stays inside fixed bars/surfaces; percentage width, text, and accessible names share one normalization; restored After-Close pages refresh once a cached deadline is due; and the homepage demo restores its 4px gap. Under Node 24.18.0, migration guard, 441 Vitest tests, type check, generated binding drift, production build, focused 28/28 vote and 17/17 Results browser suites, and a clean full 78/78 one-worker browser run passed. The default 10-worker local Playwright command reproduced Miniflare/D1 shared-SQLite internal errors; affected tests passed in isolation and in the full one-worker run.
- **`[ASSUMPTION: percentage denominator]` ratified.** Each option renders `Math.round(optionCount / voterCount * 100)` — the share of distinct Voters who selected it — for both single- and multi-select Polls. Multi-select percentages intentionally total above 100 and the `N VOTERS · M SELECTIONS` label-caps-lg line explains the shape; a zero-Voter denominator yields exactly 0, never NaN/Infinity. This resolves the deferred Story 1.1 input-safety item (`results-bar NaN/Infinity percent unhandled`), removed from `deferred-work.md` in this story.
- The Results outward view union carries the internal `pollId` on resolved (non-`not_found`) members solely so the inbound adapter can set `requestContext.pollId`; it is never rendered.
- Accessible bar names pluralize the count (`1 vote`); the pinned form (`…, 122 votes, leading`) only covers the plural case.
- The cold-load skeleton is fail-open: the server renders the authorized final Tally as the no-JS floor, and an inline enhancer (per tally instance) presents the skeleton for a painted frame, then reveals the complete final region on the next frame with no transition and no client-side fetching. Skeleton bars are named `{label}, loading`; totals, tie/empty copy, and final bars stay hidden together; and the group exposes `aria-busy="true"` until resolution.
- The post-vote ≥`lg` two-column composition uses a CSS grid on `.poll-shell[data-post-vote="true"]` with `display: contents` on the tally root, so the `YOUR BALLOT` block (owned by `results-tally.astro`) sits in the left column while summary/`TIED`/empty lines and the bar group occupy the right. Below `lg` the DOM order keeps one reading-order column; no positional copy is used.
- Read-only vote surfaces (already-voted/closed) keep the full option list with every selected/gold marker suppressed (`checked={false}`); the compact Counted composition omits the option list entirely. `findVoteSelectionByClaim` runs only after a `visible` Results outcome — never for hidden After Close or unauthorized Creator-Only.
- Reuse extractions: the local-deadline progressive enhancement moved from `vote-form.ts` into `src/scripts/deadline-time.ts` (consumed by `vote-form.ts` and the new `src/scripts/results-page.ts`); `formatUtc` moved to `src/lib/datetime.ts`. Behavior of both is unchanged.
- The direct Results route performs no preliminary `PollPage` read: the Results access read is the exact reference lookup, and only `not_found` falls through to the bounded custom-link case-fold (301 preserves `/results` and the query string; generated-reference misses stay 404). Every response is `private, no-store`; mutations are rejected with 405 before any tally read.
- No-leak e2e assertions scope to `<main>` because Astro hoists component CSS into `<head>` regardless of conditional rendering.
- `requestContext.pollId` telemetry was verified end to end by spawning a dev server and parsing its Workers Logs records: internal Poll ID present for visible and hidden resolved Polls, `null` for `not_found`, exactly the six voter-blind fields.
- No migration, dependency, binding, or token change was needed; Stories 1.9/1.10 features (polling, ETag, animation, chart toggle) are deliberately absent.

### File List

- `src/modules/results/index.ts` — Results application module: `ViewerContext`, access-envelope/tally ports, closed outward view union, `queryResults` visibility policy, percentage/leader/tie projection, `RESULTS_COPY` (replaced placeholder)
- `src/modules/results/post-vote.ts` — framework-free authorization gate that permits claim-selection reads only for visible Results with a voter token
- `src/adapters/d1/index.ts` — added `createResultsPersistence`: safe access read (no options/votes) and single-statement SQL tally projection with cross-Poll scoping and fail-closed count mapping
- `src/components/results-bar.astro` — live-data bar: one normalized width/text/accessible percentage, fixed 34/38px geometry, ellipsized labels, nonshrinking values, diamond before the value cluster, zero-width edge suppression, loading variant
- `src/components/results-bar.ts` — pure presentation helpers (`barWidthPercent`, `barAccessibleName`)
- `src/components/results-tally.astro` — shared static Tally: option order, multi-select summary, `TIED`, empty copy, wrapping text-only `YOUR BALLOT`, coherent fail-open two-frame cold-load region
- `src/pages/[reference]/results.astro` — direct Results inbound adapter (GET/HEAD, visibility-union rendering, case-fold 301, `private, no-store`, `requestContext.pollId`)
- `src/pages/[reference].astro` — exhaustive authorized Results mapping on read-only outcomes, explicit unavailable fallback/telemetry flag, fresh Counted copy, compact composition, wrapping question, marker suppression, claim lookup gated on `visible`
- `src/pages/index.astro` — refreshed stale "voting/results later" foundation copy and restored the 4px ResultsBar demo gap
- `src/scripts/deadline-time.ts` — shared local-deadline enhancement (extracted from `vote-form.ts`)
- `src/scripts/results-page.ts` — results-route deadline localization plus one-shot due-deadline refresh on BFCache restoration
- `src/scripts/vote-form.ts` — consumes the extracted deadline enhancement
- `src/lib/datetime.ts` — shared `formatUtc` (extracted from `[reference].astro`)
- `src/lib/request-context.ts` — records additive Results lookup failure for the outer telemetry middleware
- `src/adapters/telemetry/index.ts` — classifies a flagged Results lookup failure as `error` without emitting another event
- `src/middleware.ts` — initializes and forwards the Results lookup failure flag into the existing telemetry record
- `tests/unit/results.test.ts` — visibility matrix, deadline boundary, hidden-port non-invocation, denominator/rounding, leader/tie/empty rules
- `tests/unit/post-vote-results.test.ts` — structural claim-selection non-invocation proof for hidden, unavailable, missing, and tokenless Results outcomes
- `tests/unit/results-bar.test.ts` — width normalization and accessible-name helpers
- `tests/unit/telemetry.test.ts` — Results lookup failure classification regression
- `tests/integration/results-adapter.integration.test.ts` — D1 access read and tally projection against real workerd (zero votes, single/multi-select, tie, cross-Poll FK isolation, missing Poll)
- `tests/e2e/results.spec.mjs` — visibility matrix, no-leak hidden shapes, skeleton/no-JS behavior, 404/301/method safety, escaping, telemetry `pollId`, browser proof
- `tests/e2e/vote.spec.mjs` — updated to the compact/full read-only compositions, `YOUR BALLOT`, marker-suppression, and hidden no-leak assertions
- `tests/e2e/creator-session.mjs` — exported `d1Execute` for results fixtures
- `playwright.config.ts` — serializes the shared local-D1 E2E gate to one worker
- `CHANGELOG.md` — `## [Unreleased]` entry for the Results route and post-vote Tally
- `_bmad-output/implementation-artifacts/deferred-work.md` — removed the resolved Story 1.1 results-bar input-safety item
- `_bmad-output/implementation-artifacts/1-8-results-view-with-visibility-settings.md` — this story record
