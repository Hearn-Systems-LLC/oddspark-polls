---
scope: Whole-product architecture governing independently implemented Oddspark Polls epics
purpose: Build substrate for a simple public polling product serving small business owners, web developers, independent entrepreneurs, and millennial Internet nerds
altitude: feature
updated: 2026-07-29T01:43
---

- (direction) Fast path selected; infer a complete feature-altitude spine and tag unresolved calls as assumptions.
- (direction) The user defines the product audience as small business owners, web developers, independent entrepreneurs, and millennial Internet nerds; they must easily create, vote on, discover, and share polls.
- (assumption) The architecture spine is the only requested deliverable; no separate stakeholder explainer is produced unless requested at polish.
- (constraint) The finalized PRD and UX are load-bearing inputs: Cloudflare end-to-end, USD 0-5 monthly target, trustworthy server-side tabulation, anonymous voters, lightweight mobile-first public surfaces, D1 for poll and vote data, R2 for images, Turnstile, raw ballots retained, and phased poll types.
- (assumption) The user's create and discover outcomes supersede the PRD's Justin-only creator and unlisted-only constraints. Public creators authenticate with Google or GitHub through Better Auth backed by D1; voters remain anonymous. Public discovery is opt-in per poll and administratively reversible.
- (decision) Binds: all capabilities. Prevents: route handlers, persistence code, and UI independently owning business rules. Rule: use a hexagonal modular monolith; Astro routes are inbound adapters, application commands and queries orchestrate pure capability/domain modules, and only outbound ports may depend on Cloudflare or third-party adapters.
- (decision) Binds: all browser surfaces and the UX performance constraint. Prevents: a hydrated SPA and divergent client/server validation becoming the product default. Rule: start from the official Cloudflare Astro Workers starter; render HTML on the server, ship zero client JavaScript by default, and add isolated vanilla TypeScript enhancements that preserve working POST-redirect-GET flows without JavaScript.
- (decision) Binds: FR-2 through FR-14 and independently implemented poll-type epics. Prevents: one giant poll switch and incompatible lifecycle/security/results contracts. Rule: each poll type implements the same create, validate-submission, persist-facts, and project-results ports; shared lifecycle, ownership, discovery, security, comments, and result visibility wrap those strategies; poll-type facts use normalized relational rows rather than opaque JSON ballots.
- (decision) Binds: creator accounts, creator routes, poll mutation, exports, moderation, and discovery. Prevents: authentication being mistaken for authorization or one creator mutating another creator's polls. Rule: Better Auth owns identity and sessions in D1; every creator command loads the poll by poll id plus creator user id; admin moderation is a separate role capability; OAuth tokens never authorize poll mutations directly.
- (decision) Binds: public discovery, sharing, FR-20, and FR-23 replacement. Prevents: tally visibility, link reachability, and directory discoverability being conflated. Rule: store independent result_visibility and discovery_state fields; unlisted polls remain reachable by link but absent from discovery and sitemaps; only listed, policy-eligible polls enter the public directory; admins may delist without changing poll ownership or vote data.
- (decision) Binds: all persisted poll, vote, identity, comment, code, and moderation facts. Prevents: D1, R2, caches, or live transports each becoming competing truth. Rule: D1 is the sole transactional source of truth; R2 stores poll-owned image bytes only; results, manifests, and discovery cards are projections; raw accepted vote facts are immutable except the explicitly updateable meeting availability identity.
- (decision) Binds: every vote path, FR-16 through FR-18, concurrency and retry safety. Prevents: duplicate ballots from races or browser retries and partially consumed voter codes. Rule: validate external challenges before mutation, then commit vote, selections or availability, comment, duplicate claims, conditional code redemption, and poll results_version in one D1 batch guarded by unique or conditional SQL constraints; every form carries a unique submission id independent of security toggles.
- (decision) Binds: IP checks, session checks, rate-limit keys, logs, and voter privacy. Prevents: raw network or browser identifiers leaking through D1, exports, logs, or creator surfaces. Rule: normalize IPv4 and IPv6 per PRD, then store only secret-keyed HMAC digests scoped to poll and check kind; browser duplicate identity is a random first-party token digested the same way; secrets remain in Worker secrets and digests never appear in projections.
- (decision) Binds: FR-9, FR-10, FR-20 through FR-22, and all tally views. Prevents: client-authored tallies, stale aggregates, and non-reproducible ranked-choice outcomes. Rule: compute tallies server-side from accepted raw vote facts; multiple-choice and meeting results use SQL projections, ranked choice uses one pure deterministic tabulator with property tests, and closed ranked-choice manifests expose only canonically ordered anonymized rankings.
- (decision) Binds: FR-21 and the signature live-results UX. Prevents: premature paid real-time infrastructure and independently chosen refresh semantics. Rule: Phase 1 uses conditional HTTP polling of a versioned results projection while the page is visible, immediately refreshes on visibility return, coalesces intermediate versions, and stops on close; Durable Object WebSocket hibernation is deferred until measured latency or request volume justifies it.
- (decision) Binds: deadlines and manual close. Prevents: correctness depending on a cron firing on time. Rule: effective poll state is closed whenever closed_at is set or deadline is not later than the request timestamp; all reads and commands enforce effective state, while scheduled maintenance may materialize closure but is never the correctness boundary.
- (decision) Binds: image uploads and poll deletion across D1 and R2. Prevents: D1 rollback assumptions crossing into object storage and orphaned images. Rule: uploads enter poll-scoped temporary R2 keys and become visible only after D1 adoption; deletion first tombstones the D1 aggregate and records an outbox cleanup task in the same batch, then idempotent background cleanup removes R2 objects and retries until complete.
- (decision) Binds: custom links, generated links, sharing, public routes, and discovery. Prevents: route collisions and guessable generated references. Rule: canonical poll references occupy the root path to preserve the PRD contract; one shared reserved-slug registry binds routing and validation; generated references contain at least 96 random bits encoded URL-safely; canonical URLs never change when display text changes.
- (decision) Binds: deployment, schema evolution, bindings, secrets, and operational safety. Prevents: preview or staging writes reaching production and code/schema skew during rollout. Rule: local, staging, and production use separate D1, R2, auth credentials, secrets, and Worker names; Wrangler JSONC is binding truth; migrations are forward-only numbered SQL and use expand-contract compatibility before destructive cleanup; production deploys only after tests, build, and staging migration pass.
- (decision) Binds: operations, privacy, incident diagnosis, and recovery. Prevents: untraceable failures, PII in telemetry, and recovery relying on application code. Rule: enable Workers logs with structured request, operation, result, latency, and stable error code fields but no voter digests, tokens, comments, or ballot content; use D1 Time Travel as the database recovery floor and reconcile R2 objects from D1 ownership records after restore.
- (decision) Binds: abuse protection for creation, voting, auth, and discovery. Prevents: permissive edge throttles being treated as vote-integrity guarantees. Rule: Cloudflare Rate Limiting bindings and Turnstile are best-effort admission controls keyed by authenticated user, session digest, poll, and operation; only D1 constraints decide whether a vote or code redemption is accepted.
- (version) Verified 2026-07-29 against official Cloudflare, Astro, Better Auth, Node documentation and the npm registry: Node 24.18.0 LTS; pnpm 11.17.0; TypeScript 7.0.2; Astro 7.1.5; @astrojs/cloudflare 14.1.6; Better Auth 1.6.25; Zod 4.4.3; Wrangler 4.115.0; Vitest 4.1.10; @cloudflare/vitest-pool-workers 0.19.0; Playwright 1.62.0; fast-check 4.9.0; Workers compatibility date 2026-07-29.
- (question) Upstream conflict to reconcile after architecture review: update the PRD and UX from sole creator plus unlisted-only to public creator accounts plus opt-in discovery, or narrow this spine back to the finalized PRD.
- (event) Fast-path architecture decisions captured; ready to distill the draft spine.
- (direction) The user's 2026-07-29 statement is authoritative for public self-service creation and opt-in discovery; those outcomes are adopted for this spine and override the PRD's Justin-only and unlisted-only scope. The concrete identity mechanism remains open.
- (decision) Binds: FR-5 and image-backed Poll deletion. Prevents: a tombstone being mistaken for deletion. Rule: record self-contained R2 cleanup keys and hard-delete the Poll plus D1 children in one batch; retry idempotent R2 cleanup from the independent outbox.
- (decision) Binds: FR-2, FR-5, FR-15, and FR-24. Prevents: later flows changing the contract accepted voters saw. Rule: no draft; after the first accepted Vote lock question, options, and Poll Type; allow description edits; Security Toggles only tighten; Comments are one-per-Vote plain text in the Vote transaction and follow result visibility.
- (decision) Binds: all platform and provider choices. Prevents: incremental services silently breaking the USD 0-5 monthly ceiling. Rule: baseline production must fit Cloudflare free tiers or one USD 5 Workers Paid plan; any additional mandatory fee requires a new architecture decision.
- (decision) Binds: CAP-SHARE and all public Poll views. Prevents: stable URLs existing without an easy sharing interaction. Rule: voting, confirmation, and result views render the canonical URL and Share action, enhanced with Web Share API and copy-link fallback.
- (decision) Binds: FR-21. Prevents: a disconnected browser silently presenting stale results as live. Rule: poll every three seconds while visible; show RECONNECTING on first failure, preserve last truth, retry with capped 30-second backoff, and refresh immediately on visibility or connectivity return.
- (decision) Binds: all browser mutations. Prevents: failed validation discarding a ballot. Rule: successful POSTs return 303; validation failures return 422 and re-render safe submitted values plus field errors.
- (question) Confirm creator identity mechanism before finalization: Better Auth with Google plus GitHub, a single provider, or another ownership mechanism.
- (event) PRD and UX reconciliation completed; clear architecture fixes applied; upstream PRD and UX scope synchronization deferred until the new public creation and discovery direction is adopted there.
- (decision) Binds: Vote acceptance, Poll close, Poll delete, and deadlines. Prevents: a Vote committing against stale open state. Rule: a D1 Vote-insert trigger checks effective open state inside the write transaction and foreign keys make concurrent Poll deletion abort the Vote.
- (decision) Binds: result, Comment, Manifest, export, and discovery reads. Prevents: restricted facts leaking through shared projections, ETags, or caches. Rule: authorize ViewerContext before projection; no result or Manifest shared caching; discovery uses a separate public-field-only projection and cache namespace.
- (decision) Binds: all D1 facts. Prevents: multiple modules mutating the same fact with incompatible rules. Rule: Identity owns auth facts; Polls owns lifecycle/config/options; Discovery owns listing/moderation; Voting owns vote facts/claims/codes/comments; Media owns media/outbox; Results is read-only; CastVote is the sole cross-module transaction coordinator.
- (decision) Binds: Meeting Poll revisions and vote security. Prevents: availability updates creating duplicate claims or consuming codes twice. Rule: initial Meeting response creates Vote/claims/redemption and a revision capability; revisions require that capability and replace only availability rows without new claims or redemption.
- (decision) Binds: R2 adoption and deletion cleanup. Prevents: abandoned temporary keys and deleted-Poll media surviving indefinitely. Rule: deletion outbox is self-contained and independent of Poll foreign keys; same-Worker Cron drains every 15 minutes; waitUntil is only acceleration; temporary objects older than 24 hours are swept.
- (decision) Binds: creator/admin and anonymous browser mutations. Prevents: cross-site request forgery. Rule: central middleware enforces same-origin Origin and Fetch Metadata on every state-changing route; authenticated forms also require a session-bound CSRF token; Better Auth retains its built-in CSRF and OAuth state checks.
- (decision) Binds: discovery state. Prevents: owner and moderator transitions diverging. Rule: Discovery owns unlisted/listed/delisted; owners toggle unlisted/listed; admins may delist and exclusively clear delisted; directory returns only effectively open listed Polls.
- (decision) Binds: idempotent Vote retry. Prevents: a committed Vote replay becoming an error or second Vote. Rule: normalize payload and check unique poll plus submission id before external challenge; exact replay returns stored outcome, mismatched payload conflicts; new submissions validate challenges then transact.
- (decision) Binds: Voter Code redemption. Prevents: zero-row conditional updates allowing a Vote to commit. Rule: insert a redemption row uniquely keyed by code id with foreign keys to code and Vote in the Vote batch, so invalid or reused codes abort the batch.
- (decision) Binds: Cloudflare runtime configuration for auth. Prevents: assumed Better Auth failing under Workers. Rule: wrangler.jsonc enables nodejs_compat and owns the Workers compatibility date.
- (decision) Binds: CAPTCHA-enabled Votes. Prevents: best-effort language making Turnstile optional. Rule: Rate Limiting is permissive admission throttling, but enabled Turnstile validates server-side and fails closed on missing, invalid, duplicate, expired, or unverifiable tokens.
- (decision) Binds: database recovery. Prevents: overstating recovery history. Rule: D1 Time Travel is the recovery floor with a 7-day window on Free or 30-day window on Paid; restore reconciliation uses D1 media ownership records to repair R2.
- (event) Reviewer Gate completed: rubric, current-technology, and adversarial reviews written under reviews/. All clear critical/high findings applied; creator auth mechanism and upstream PRD/UX synchronization remain for user resolution.
- (decision) Binds: new Poll discovery. Prevents: creator and discovery epics choosing incompatible privacy defaults. Rule: every Poll starts unlisted and creation requires explicit opt-in to listed.
- (decision) Binds: Meeting response revision, Poll close, and Poll delete. Prevents: a revision committing after close/delete. Rule: D1 triggers enforce effective-open state on availability replacement and foreign keys abort concurrent parent deletion.
- (decision) Binds: all shared IDs, enums, Poll Type contributions, and serialized contracts. Prevents: modules defining incompatible shapes. Rule: shared/domain owns branded IDs and shared enums; shared/application owns versioned contribution and error contracts; capabilities own projections; adapters never expose database rows.
- (decision) Binds: live results and HTTP validators. Prevents: non-Vote changes remaining invisible behind a successful ETag. Rule: one monotonic representation_version increments for every representation-changing mutation; response validators also include effective state so Deadline crossing invalidates without a write.
- (decision) Binds: Poll creation and every Poll Type. Prevents: a reachable partial Poll. Rule: CreatePoll validates shared fields, receives normalized facts from exactly one type strategy, and batches Poll, type facts, options or slots, slug reservation, and media adoption atomically.
- (decision) Binds: creator ownership. Prevents: provider identity changes orphaning Polls or causing collisions. Rule: Polls reference an internal provider-independent user id; OAuth provider/account pairs map to users and never serve as ownership keys.
- (decision) Binds: R2 media replacement. Prevents: replacing an adopted image orphaning its prior object. Rule: each media row singly owns an immutable R2 key; replacement atomically updates the reference and enqueues the superseded key for cleanup.
- (event) Reviewer verification found a second adversarial layer; all reported high-severity seams were amended in the spine.
- (event) Reviewer Gate verification passed: rubric, current-technology, and adversarial reviewers report no remaining critical or high findings; deterministic lint reports zero findings.
- (decision) User approved the creator identity mechanism: Better Auth 1.6.25 backed by D1 with Google and GitHub OAuth; AD-4 is adopted and no architecture assumption remains.
- (event) Handoff prepared for another agent after Better Auth approval; spine remains draft pending blocking PRD and UX scope synchronization, reconciliation, and finalization.
- (event) PRD and UX packages synchronized with the approved public-product direction (public creators via Better Auth, opt-in discovery, Share actions); both re-reconciliation reports pass with no new critical or high findings
- (decision) Binds: traceability to updated sources. Prevents: stale FR/UJ ranges and a replaced-FR-23 framing surviving finalization. Rule: spine binds FR-1..FR-28 and UJ-1..UJ-7; FR-23 is the opt-in discovery requirement itself; FR-28 folds into CAP-SHARE; the blocking PRD/UX synchronization deferral is removed; creator account deletion is deferred with a pre-launch revisit condition.
- (event) spine finalized
