---
import "../styles/fonts.css";
import "../styles/tokens.css";

interface Props {
  title?: string;
  description?: string;
  canonicalUrl?: string;
  robots?: string;
}

const {
  title = "oddspark polls",
  description = "Trustworthy casual polls — free, full-featured, no subscription.",
  canonicalUrl,
  robots,
} = Astro.props;

const ogImageUrl = new URL("/og-image.png", canonicalUrl ?? Astro.url.origin).href;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    {canonicalUrl && <link rel="canonical" href={canonicalUrl} />}
    {robots && <meta name="robots" content={robots} />}
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta property="og:site_name" content="oddspark polls" />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    {canonicalUrl && <meta property="og:url" content={canonicalUrl} />}
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:image" content={ogImageUrl} />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <!-- Anti-flash mode: apply stored override before paint when JS is available -->
    <script is:inline>
      (function () {
        try {
          var stored = localStorage.getItem("oddspark-mode");
          if (stored === "light" || stored === "dark") {
            document.documentElement.setAttribute("data-mode", stored);
          }
        } catch (_) {}
      })();
    </script>
  </head>
  <body>
    <slot />
    <script src="../scripts/mode-override.ts"></script>
  </body>
</html>
