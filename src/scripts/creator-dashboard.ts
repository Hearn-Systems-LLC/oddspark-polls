// Creator dashboard / detail enhancement: localize absolute deadline floors
// in poll-card captions. Server markup remains complete without this script.

import { enhanceDeadlineTimes } from "./deadline-time";

enhanceDeadlineTimes();
